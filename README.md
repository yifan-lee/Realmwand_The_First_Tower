# Realmwand: The First Tower

Godot 4.7 早期开发项目。

项目已于 2026-07-30 清空旧游戏实现，从最小可运行基线重新开始。

当前开发方式：

- 场景负责设计期节点树、布局和 Inspector 配置。
- GDScript 负责运行时行为、状态变化和信号处理。
- `.tres` 只保存共享定义，不保存运行时状态。
- 每个开发阶段先完成场景和数据，再编写行为，最后运行回归与目标验收。

架构规划见 `docs/architecture.md`。
