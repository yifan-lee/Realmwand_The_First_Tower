# 2026-07-27 开发进度

### 更新时间

2026-07-30 15:21 EDT

### 更新内容

- 完成从零重建的第 0 块：保留 `assets/`、项目工具链与 Godot MCP Pro，移出旧场景、脚本、玩法资源、旧文档及旧测试产物。
- 旧内容已做可恢复备份：`/private/tmp/realmwand-pre-rebuild-20260730-E5Ked5/`。
- 建立约定的空目录骨架，后续按楼层、角色、交互物、战斗、UI、数据资源等模块逐块实现。
- 建立最小入口场景 `scenes/main.tscn`，包含 `World/FloorContainer`、`Systems`、`OverlayRoot` 三个顶层容器。
- 建立最小入口脚本 `scripts/main.gd`，当前不承载玩法逻辑。
- 将项目主场景设置为 `res://scenes/main.tscn`。
- 重写基础说明、架构说明与开发规范，并更新临时测试产物的忽略规则。
- 清理 Godot 的旧编辑器缓存，避免已移出场景在启动时产生历史资源错误。

### 测试结果

通过

- 基础回归：8 项通过，0 项失败。
- `scripts/main.gd` 编译通过。
- 主场景能够启动和停止，运行时根节点为 `Main`。
- 编辑器在正式回归前后均为 0 个错误。
- 主场景截图为预期的空白重建基线；第 0 块不包含玩法、地图或 UI。

### 证据

- 基础回归报告：`tmp/tests/run-20260730-152056/reports/basic_regression.md`
- 基础回归数据：`tmp/tests/run-20260730-152056/reports/basic_regression.json`
- 运行截图：`tmp/tests/run-20260730-152056/screenshots/main_scene.png`
- 场景入口：`scenes/main.tscn`
- 架构说明：`docs/architecture.md`

---

### 更新时间

2026-07-30 17:26 EDT

### 更新内容

- 生成六张高分辨率技能概念表并保存到 `assets/skills/source/`。
- 切分出 100 张统一为 343×343 透明画布的独立技能图标：
  物理攻击32张、法术攻击48张、辅助法术20张。
- 每个技能族包含 `tier_1` 至 `tier_4` 四级，视觉复杂度、能量强度和
  传奇边框逐级增强。
- 辅助法术按属性使用固定视觉语言：ATK红、DEF黄、SPD蓝、HP绿、
  MP紫青。
- 新增 `tools/build_skill_icons.py` 和 `assets/skills/skills_manifest.json`，
  支持按完整卡片区域重复切分并记录类别、技能族、等级和源坐标。

### 测试结果

通过

- 基础回归复跑：8项通过，0项失败。
- 技能图标构建工具和Python语法检查通过。
- 物理、法术、辅助目录数量分别为32、48、20，总计100张。
- 全部图标均为343×343 RGBA PNG，主体非空且未接触画布边缘。
- Godot项目文件系统能够识别 `res://assets/skills` 下的全部PNG。
- 视觉检查确认物理、法术和辅助类别可区分，四级递进明显；
  HP绿色与MP紫青色在透明化后保持完整。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260730-172132/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260730-172132/reports/basic_regression.json`
- 技能图标总览：
  `tmp/tests/run-20260730-skill-icons/screenshots/skill_icons_overview.png`
- 技能清单：
  `assets/skills/skills_manifest.json`

---

### 更新时间

2026-07-30 19:44 EDT

### 更新内容

- 完成第一块数据层：建立 Actor、Player、Enemy、Item、Equipment、Skill
  与 SkillEffect 的类型资源和首批 `.tres` 数据。
- 建立 `scenes/actors/player.tscn`，固定节点树包含 AnimatedSprite2D、
  CollisionShape2D、InteractionRay 和 Camera2D。
- 建立外部 `player_sprite_frames.tres`，配置四方向 Idle 与 Walk 动画，
  并由 PlayerData 和 Player 场景共同引用。
- 建立 PlayerData 驱动的运行时初始化，运行时 HP、MP、等级、金币、
  背包和技能不写回共享 Resource。
- 实现 32 像素四方向格子移动、方向动画、交互射线方向更新、输入锁和
  统一的 `interact(player)` 调用入口。
- 将 Player 场景作为固定实例放入 Main 的 World，FloorContainer 保留为
  后续动态楼层容器。

### 测试结果

通过

- 基础回归：8 项通过，0 项失败。
- 运行时场景树包含 Main、World、FloorContainer、Player 及 Player 的
  四个固定子节点。
- Player 运行时属性与 `player.tres` 一致：HP 400、MP 200、ATK 40、
  DEF 40、SPD 40、等级 1。
- 依次向右、下、左、上移动后，坐标为 `(32, 0)`、`(32, 32)`、
  `(0, 32)`、`(0, 0)`，并分别回到对应方向的 Idle 动画。
- `input_enabled = false` 时方向输入不会改变 Player 坐标。
- 运行时修改 current_hp 和 gold 后，`player.tres` 的 Max HP 与
  Starting Gold 保持不变。
- 无目标时执行交互不会报错；停止运行后编辑器错误为 0。
- 视觉检查确认 Player 位于 Camera 中央，Sprite、碰撞体和交互射线能够
  正常显示；楼层背景尚未实现，灰色背景符合当前阶段。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260730-194004/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260730-194004/reports/basic_regression.json`
- 基础回归截图：
  `tmp/tests/run-20260730-194004/screenshots/main_scene.png`
- Player 专项视觉截图：
  `tmp/tests/run-20260730-194004/screenshots/player_validation.png`
- Player 场景：`scenes/actors/player.tscn`
- Player 脚本：`scripts/actors/player.gd`
- Player 数据：`resources/actors/player.tres`

---

### 更新时间

2026-07-30 21:05 EDT

### 更新内容

- 对 Main 启动后未加载第一层的问题进行只读诊断。
- 确认 `FloorManager` 能够加载并实例化 `floor_1.tscn`，但实例的
  `floor_id` 仍为空，未通过与目标 ID `floor_1` 的一致性检查，因此在
  加入 `FloorContainer` 前被释放。
- 发现 `WallSwitch.state_changed` 的场景连接方法名与
  `floor_1.gd` 中现有处理函数名不一致；该问题不阻止首次楼层加载，
  但会阻止后续 Switch 信号调用。

### 测试结果

失败

- 基础回归：6 项通过，2 项失败。
- Main 场景能够启动、截图并读取运行时场景树。
- BR-003 与 BR-008 因编辑器错误
  `Floor ID '' does not match requested ID 'floor_1'.` 失败。
- 根据基础回归门禁，本轮未继续 Switch 或楼层交互专项验证。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260730-210437/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260730-210437/reports/basic_regression.json`
- 启动截图：
  `tmp/tests/run-20260730-210437/screenshots/main_scene.png`
- 相关场景：`scenes/floors/floor_1.tscn`
- 相关脚本：`scripts/floors/floor_manager.gd`

---

### 更新时间

2026-07-30 21:18 EDT

### 更新内容

- 对 Player 面向 WallSwitch 按交互键后墙壁不变化的问题进行只读诊断。
- 确认 Player、FloorSwitch、楼层信号连接及三格墙的缓存/恢复逻辑均可
  正常运行。
- 定位到 WallSwitch 与边界墙格重叠：Player 的 InteractionRay 会先命中
  WallLayer 的完整格碰撞，而不是较小的 `24×24` Switch 交互区。
- 仅在运行时临时将 Switch 碰撞范围调整为 `40×40` 进行验证；该测试
  状态未写回场景或资源。

### 测试结果

通过

- 基础回归：8 项通过，0 项失败。
- 初始射线命中对象为 `WallLayer`，证明交互在到达 Switch 前被墙截断。
- 直接调用 Switch 交互后，`is_active` 变为 `true`，三个目标格子的
  Source ID 均从 `0` 变为 `-1`。
- 运行时将 Switch 碰撞范围临时设为 `40×40` 后，射线命中对象变为
  `WallSwitch`；模拟交互输入可将状态切回 `false` 并恢复三格墙。
- 停止场景后编辑器错误为 0。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260730-211615/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260730-211615/reports/basic_regression.json`
- 基础回归截图：
  `tmp/tests/run-20260730-211615/screenshots/main_scene.png`
- Switch 场景：`scenes/interactables/switch.tscn`
- 第一层场景：`scenes/floors/floor_1.tscn`

---

### 更新时间

2026-07-30 22:17 EDT

### 更新内容

- 尝试开始第3块楼层往返、重叠出生点与 Switch 状态恢复的完整验证。
- 确认当前稳定错误仅来自第一层新增但尚未配置地形规则的
  `switch_red`、`switch_blue`、`switch_yellow`。
- 按基础回归门禁，在项目仍包含 Godot `ERROR` 时停止专项验证；本轮
  未修改三个 Switch 或游戏代码。

### 测试结果

阻断

- 基础回归：6 项通过，2 项失败。
- Main 能够启动、截图并读取运行时场景树。
- BR-003 与 BR-008 因三个未配置 Switch 产生的 `push_error()` 失败。
- 按 `godot-mvp-test` 顺序要求，未继续执行楼层往返与状态恢复专项测试。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260730-221644/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260730-221644/reports/basic_regression.json`
- 基础回归截图：
  `tmp/tests/run-20260730-221644/screenshots/main_scene.png`
- 错误来源：`scripts/floors/floor_1.gd`

---

### 更新时间

2026-07-30 22:25 EDT

### 更新内容

- 完成第3块楼层往返、自动楼梯、重叠出生点与楼层状态恢复的专项验证。
- 验证第一层 `wall_passage` Switch 可同时移除三个目标墙格。
- 验证进入楼梯后自动从第一层切换到第二层，并可从第二层返回第一层。
- 验证出生点与楼梯重叠时不会在加载后立即反向切层。
- 验证返回第一层后 Switch 激活状态和三个已移除墙格均得到恢复。
- 未修改任何游戏代码、场景或资源；专项测试中的 Player 位置调整仅存在
  于运行时。

### 测试结果

失败

- 基础回归：8 项通过，0 项失败。
- Switch 切换后，三个目标墙格 Source ID 均从 `0` 变为 `-1`。
- 第一层切换到第二层后，Player 出现在 `FromBelowStair`，等待后没有
  立即弹回第一层。
- Player 离开并重新进入二楼楼梯后成功返回第一层的
  `FromAboveStair`。
- 返回第一层后 `wall_passage` 保持激活，三个目标墙格仍为 `-1`。
- 视觉检查发现第二层用于返回第一层的楼梯仍显示向上箭头；其
  `direction` 尚未配置为 `DOWN`，因此本轮目标未完全通过。
- 场景停止后编辑器错误为 0。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260730-221935/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260730-221935/reports/basic_regression.json`
- 第一层返回与状态恢复截图：
  `tmp/tests/run-20260730-221935/screenshots/floor_1_return_state.png`
- 第二层重叠出生点截图：
  `tmp/tests/run-20260730-221935/screenshots/floor_2_spawn_overlap.png`
- 第二层场景：`scenes/floors/floor_2.tscn`

---

### 更新时间

2026-07-31 09:38 EDT

### 更新内容

- 建立 Player 的独立 Inventory 运行时组件和通用 ItemPickup 场景。
- ItemPickup 可从 ItemData 自动取得世界贴图，并在 Player 进入触发区时
  将物品加入 Inventory。
- Floor 运行时状态扩展为同时保存 Switch 与 Pickup 状态。
- 对自动拾取、背包数量变化和 Pickup 持久化进行专项验证；本轮只执行
  只读检查和运行时测试，未修改游戏代码、场景或资源。

### 测试结果

失败

- 基础回归：8 项通过，0 项失败。
- Player 进入 `HpRecoveryPickup` 后位置到达 `(0, -64)`，
  `hp_recovery_lv1` 数量从 0 增加到 1，证明自动拾取链路通过。
- Pickup 在调用 `set_collected(true)` 后仍继续执行 `queue_free()`，运行时
  节点被删除，因此 Floor 离开时无法捕获其 `is_collected` 状态，楼层
  往返持久化未通过。
- `_get_configuration_warnings()` 当前无条件添加 Pickup ID 警告，即使
  `pickup_id` 已配置也会显示黄色警告。
- 专项查询已删除节点时触发一条 MCP 调试器自动恢复警告；该警告来自
  验收查询，不是游戏运行逻辑错误。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260731-093633/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260731-093633/reports/basic_regression.json`
- 基础回归截图：
  `tmp/tests/run-20260731-093633/screenshots/main_scene.png`
- Pickup 脚本：`scripts/interactables/item_pickup.gd`
- Floor 状态脚本：`scripts/floors/floor.gd`

---

### 更新时间

2026-07-31 10:29 EDT

### 更新内容

- 将敌人的地图贴图从具体 Enemy 场景迁移到 EnemyData 的
  `world_texture` 字段。
- Enemy 脚本根据导出的 EnemyData 自动刷新 Sprite2D，统一普通物品、
  装备和敌人的数据驱动视觉配置方式。
- 具体 `enemy_balance_lv1.tscn` 现在只保存公共 Enemy 场景和 EnemyData
  引用，不再保存重复的 Texture 覆盖。
- 完成资源引用、运行时贴图一致性和视觉专项验证；本轮未修改游戏代码、
  场景或资源。

### 测试结果

通过

- 基础回归：8项通过、0项失败。
- `enemy_balance_lv1.tres.world_texture` 正确引用
  `assets/monsters/balanced_tier_1.png`。
- 运行时 Enemy 的 `Sprite2D.texture` 与
  `EnemyData.world_texture` 为同一个资源实例。
- 具体 Enemy 场景文件只包含 EnemyData 引用，没有 Sprite2D Texture
  覆盖。
- 视觉检查确认敌人仍在地图中正确显示，装备和普通物品 Pickup 也正常
  显示。
- 场景停止后编辑器错误为0。
- `enemy.gd` 的配置警告函数仍留有一个未使用的 `sprite` 局部变量，建议
  后续删除；该冗余不影响当前功能和测试结果。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260731-102842/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260731-102842/reports/basic_regression.json`
- 运行截图：
  `tmp/tests/run-20260731-102842/screenshots/main_scene.png`
- EnemyData：`resources/actors/enemy_balance_lv1.tres`
- 具体 Enemy 场景：`scenes/actors/enemies/enemy_balance_lv1.tscn`

---

### 更新时间

2026-07-31 10:44 EDT

### 更新内容

- 建立 Player 的 EquipmentLoadout 组件，定义防具、左右手、双手武器和
  饰品槽位规则。
- Player 增加 Inventory 与 Equipment 之间的装备、换装和卸装协调入口，
  并通过基础属性加装备加成计算最终 HP、MP、ATK、DEF 和 SPD。
- Inventory 增加剩余堆叠容量查询，换装前预检被替换装备能否安全返回
  背包，避免装备丢失。
- Floor 改为从独立 `Pickups` 容器捕获和恢复 Pickup 状态，与
  `Interactables`、`Enemies` 的职责分开。
- 使用通用 ItemPickup 放置 `blade_lv1` 并完成装备模块检查点验收；
  本轮未修改游戏代码、场景或资源。

### 测试结果

通过

- 基础回归：8项通过、0项失败。
- Player 向左进入 Blade Pickup 后，`blade_lv1` 数量由0变为1，Pickup
  隐藏并停止监测。
- 刀装备到 Head 返回 `false`，背包数量保持1；装备到 Left Hand 返回
  `true`，背包数量变为0，最终 ATK 由40变为45。
- 从 Left Hand 卸装后背包数量恢复为1、ATK恢复为40；随后装备到
  Right Hand 成功，ATK再次变为45。
- 切到第二层时 FloorManager 保存 Blade Pickup 的已拾取状态；Player
  的右手装备、背包数量和45 ATK 均保持不变。
- 返回第一层后 Blade Pickup 仍隐藏且停止监测；Player 再次进入原物品
  格后没有重复获得物品。
- 人为将同一装备堆叠填满后尝试卸装返回 `false`，装备仍保留在右手且
  ATK维持45，证明容量不足时不会丢失装备。
- 视觉检查确认拾取前刀显示在左侧地图格，拾取并切层返回后该格不再
  显示刀。
- 场景停止后编辑器错误为0。
- 当前 Blade Pickup ID 为 `blade_lv1`，在本层只有一个实例时有效；若
  增加同类 Pickup，应改用 `blade_01`、`blade_02` 等实例唯一 ID。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260731-104202/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260731-104202/reports/basic_regression.json`
- 初始地图截图：
  `tmp/tests/run-20260731-104202/screenshots/main_scene.png`
- Blade 拾取后截图：
  `tmp/tests/run-20260731-104202/screenshots/blade_picked_up.png`
- 切层返回并保持装备后的截图：
  `tmp/tests/run-20260731-104202/screenshots/blade_persisted_equipped.png`
- Equipment 脚本：`scripts/inventory/equipment_loadout.gd`
- Inventory 脚本：`scripts/inventory/inventory.gd`
- Player 脚本：`scripts/actors/player.gd`

---

### 更新时间

2026-07-31 09:42 EDT

### 更新内容

- 修正 ItemPickup 完全拾取后仍被释放的问题，使其保留为隐藏的运行时
  节点，并由 Floor 捕获和恢复状态。
- 修正 Pickup ID 配置警告，使其只在 `pickup_id` 为空时出现。
- 重新验证自动拾取、Inventory 数量、切层状态保存和返回后的防重复
  拾取；本轮未修改游戏代码、场景或资源。

### 测试结果

通过

- 首次基础回归在启动前读取到2条旧日志错误，运行停止后错误归零；清洁
  状态复跑基础回归为8项通过、0项失败。
- 初始 Pickup 可见且处于监测状态，Player 的
  `hp_recovery_lv1` 数量为0。
- Player 进入 Pickup 后数量变为1；Pickup 节点仍然存在，
  `is_collected = true`、`visible = false`、`monitoring = false`。
- FloorManager 保存的 `floor_1` 状态包含
  `pickups.hp_recovery_01.is_collected = true` 和剩余数量数据。
- 切换到第二层并返回第一层后，Pickup 仍隐藏且停止监测；Player 再次
  走入同一格后物品数量仍为1，没有重复拾取。
- 视觉检查确认拾取前显示心形物品，返回后对应地图格不再显示该物品。
- 场景停止后编辑器错误为0。

### 证据

- 首次基础回归报告：
  `tmp/tests/run-20260731-094006/reports/basic_regression.md`
- 通过的基础回归报告：
  `tmp/tests/run-20260731-094034/reports/basic_regression.md`
- 通过的基础回归数据：
  `tmp/tests/run-20260731-094034/reports/basic_regression.json`
- 拾取前截图：
  `tmp/tests/run-20260731-094034/screenshots/main_scene.png`
- 拾取后截图：
  `tmp/tests/run-20260731-094034/screenshots/pickup_collected.png`
- 返回楼层后的持久化截图：
  `tmp/tests/run-20260731-094034/screenshots/pickup_persisted_after_return.png`

---

### 更新时间

2026-07-31 10:03 EDT

### 更新内容

- 建立 Enemy 公共场景和脚本，并通过继承场景配置第一种具体敌人。
- Enemy 使用 EnemyData 初始化运行时 HP/MP，具体敌人场景保存世界贴图，
  楼层实例使用唯一 `instance_id`。
- Floor 运行时状态扩展为保存敌人的 HP、MP 和击败状态。
- 完成 Enemy 数据、视觉、地图碰撞、伤害、击败和切层持久化专项验证；
  本轮未修改游戏代码、场景或资源。

### 测试结果

通过

- 基础回归：8项通过、0项失败。
- 具体敌人加载 `enemy_balance_lv1`，运行时 HP 为80、MP为10，世界贴图
  和 `(0.2, 0.2)` 缩放均正确加载。
- Player 尝试向右进入敌人格时位置保持 `(0, 0)`，InteractionRay 命中
  Enemy，证明地图阻挡和交互检测正常。
- 造成30点伤害后敌人 HP 变为50；切到第二层再返回后仍为50。
- 再造成致命伤害后实际伤害为50，HP归零，敌人隐藏并将碰撞层设为0。
- 击败状态保存为 `floor_1_enemy_01.is_defeated = true`；切层返回后仍
  隐藏且无碰撞，Player 可以进入原敌人格 `(32, 0)`。
- 视觉检查确认敌人主体大小适合单个地图格，击败后从地图画面消失。
- 示例节点名与实际节点名不同导致一次 MCP 查询警告；干净启动并停止
  场景后编辑器错误为0，项目逻辑未产生错误。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260731-100059/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260731-100059/reports/basic_regression.json`
- 敌人击败前截图：
  `tmp/tests/run-20260731-100059/screenshots/enemy_before_defeat.png`
- 敌人击败后截图：
  `tmp/tests/run-20260731-100059/screenshots/enemy_defeated.png`
- Enemy 公共脚本：`scripts/actors/enemy.gd`
- Floor 状态脚本：`scripts/floors/floor.gd`

---

### 更新时间

2026-07-31 11:25 EDT

### 更新内容

- 重建 `resources/themes/game_ui_theme.tres`，将通用 UI 视觉调整为米白色
  块、近黑色粗边框、2px 小圆角和无阴影的几何极简样式。
- 在 Theme 内嵌跨平台中文系统字体回退列表，默认字号为24px，并建立
  标题、数值、弱化文字及深色面板文字层级。
- 补齐 Button、LineEdit、ProgressBar、RichTextLabel、Tooltip、水平/
  垂直分隔线和常用 Container 的颜色、字号、间距及状态样式。
- 建立 ATK、DEF、SPD、HP、MP 五组语义 Label 与 ProgressBar 变体，
  普通边框不再使用代表 SPD 的蓝色。
- 通过纯运行时临时 HUD 同时预览浅色属性面板和深色物品面板；预览节点
  未保存到任何场景。
- 按用户约束，本轮持久化修改仅涉及主题资源和本进度文档。

### 测试结果

通过

- 最终基础回归：8项通过、0项失败。
- Theme 以忽略缓存方式重新加载成功，包含31个类型或类型变体；默认
  字体为 SystemFont，默认字号为24px。
- PanelContainer 验证为 `#F2ECE0F2` 背景、`#16181BFF` 边框、5px
  边宽和2px圆角。
- 五种属性进度条均使用独立语义色并保留完整黑色轮廓；浅色/深色面板
  上的中英文文字均可辨认。
- 临时预览与当前地图同时显示，视觉检查确认色块、粗线和直角结构与
  当前几何符号化极简科幻风格一致。
- 停止场景后编辑器错误为0。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260731-112407/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260731-112407/reports/basic_regression.json`
- 最终主题预览：
  `tmp/tests/run-20260731-game-ui-theme/screenshots/game_ui_theme_final_preview.png`
- 主题资源：`resources/themes/game_ui_theme.tres`

---

### 更新时间

2026-07-31 11:47 EDT

### 更新内容

- 完成统一辅助 UI 的首个可复用组件检查点：ActorStatsPanel。
- 检查 `ActorStatsViewData` 与面板脚本的数据边界、节点引用、文本更新、
  头像显隐和清空逻辑。
- 通过纯运行时临时 CanvasLayer 将面板覆盖在 Main 上，分别注入 Player
  与 Enemy 的实际运行时数据；测试节点未保存到 Main 场景。
- 按用户约束，本轮未修改游戏代码、场景或资源，仅更新本进度文档。

### 测试结果

通过

- 基础回归：8项通过、0项失败。
- Player 数据显示为 `400 / 400 (+20)`、`200 / 200 (-10)`、
  `40 (+5)`、`40 (-5)`、`40`，零增量不会附加括号。
- Player 的 portrait 为空时头像节点自动隐藏，标题和属性网格正常回流。
- Enemy 从 EnemyData 显示名称、80/80 HP、10/10 MP、14 ATK、6 DEF、
  20 SPD，并正确加载 `balanced_tier_1.png` 头像。
- `clear_stats()` 会清空全部文本、清除头像纹理并隐藏头像节点。
- 视觉检查确认中英文文字、米白面板、近黑粗边框、分隔线、对齐与间距
  均正常；干净启动并停止 Main 后编辑器错误为0。
- 目标测试期间一次临时 MCP 脚本因等待帧的写法中断；移除等待后相同
  数据验证通过，该警告未来自项目代码。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260731-114332/reports/basic_regression.md`
- 基础回归数据：
  `tmp/tests/run-20260731-114332/reports/basic_regression.json`
- Player 属性面板截图：
  `tmp/tests/run-20260731-114332/screenshots/player_stats_panel.png`
- Enemy 属性面板截图：
  `tmp/tests/run-20260731-114332/screenshots/enemy_stats_panel.png`
- 面板场景：`scenes/ui/components/actor_stats_panel.tscn`
- 面板脚本：`scripts/ui/components/actor_stats_panel.gd`
- 显示数据脚本：`scripts/ui/components/actor_stats_view_data.gd`

---

### 更新时间

2026-07-31 12:10 EDT

### 更新内容

- 验证可复用 InventoryPanel 与 InventoryItemRow 场景、动态刷新和选择信号。
- 通过独立运行面板场景注入三种实际 ItemData；未修改游戏代码、场景或资源。

### 测试结果

失败

- 基础回归：8项通过、0项失败；独立场景停止后编辑器错误为0。
- 空背包提示、物品排序、图标、名称、数量、增减刷新和清空恢复均正常。
- 鼠标点击第一行后，面板正确发出 `hp_recovery_lv1` 选择信号。
- `TitleLabel` 与 `EmptyLabel` 使用了主题中不存在的 `HeaderLarge` 变体；
  当前回退为普通 Label，需分别改成 `HeaderLabel` 与 `MutedLabel` 后复验。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260731-120756/reports/basic_regression.md`
- 空背包截图：
  `tmp/tests/run-20260731-120756/screenshots/inventory_empty.png`
- 已填充背包截图：
  `tmp/tests/run-20260731-120756/screenshots/inventory_populated.png`
- 面板场景：`scenes/ui/components/inventory_panel.tscn`
- 物品行场景：`scenes/ui/components/inventory_item_row.tscn`

---

### 更新时间

2026-07-31 12:12 EDT

### 更新内容

- 复验 InventoryPanel 的标题与空背包提示主题变体修正。
- 本轮未修改游戏代码、场景或资源，仅更新进度记录。

### 测试结果

通过

- 基础回归：8项通过、0项失败。
- `TitleLabel` 已使用 `HeaderLabel`，`EmptyLabel` 已使用 `MutedLabel`。
- 独立运行场景的视觉层级正确，停止后编辑器错误为0。
- 上一轮已通过的动态刷新、数量显示和物品选择逻辑保持有效。

### 证据

- 基础回归报告：
  `tmp/tests/run-20260731-121205/reports/basic_regression.md`
- 修正后截图：
  `tmp/tests/run-20260731-121205/screenshots/inventory_theme_fixed.png`
- 面板场景：`scenes/ui/components/inventory_panel.tscn`
