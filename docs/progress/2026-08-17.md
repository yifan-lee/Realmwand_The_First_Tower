# 2026-08-17

### 更新时间

2026-08-17

### 更新内容

- 完成了全局属性面板（`ActorStatsPanel`）与 Stat UI 体系的架构重构：
  - **统一数据工厂（`ActorStatsFactory`）**：集中读取 `Player`、`Enemy`（含 `Boss`）的名称、头像、描述、等级经验、HP/MP/FP、ATK/DEF/SPD、FP 恢复速度、Start FP、CP 等核心数据；自动合并 `ActorStatsContext`（战斗有效属性、Buff、ATB）与 `ActorStatsPreviewData`（预览预期差值），保持 Actor 与 Resource 实例的纯净性（零修改共享数据）。
  - **强类型配置与上下文（`ActorStatsDisplayProfile` / `ActorStatsContext` / `ActorStatsPreviewData`）**：
    - `ActorStatsDisplayProfile` 提供 `hud()`、`menu()`、`battle_player()`、`battle_enemy()`、`progression()` 预设，精细控制 FP 模式（条/恢复速度）、ATB 模式（隐藏/条/外部）、等级经验、Buff、CP 及 `FeatureUnlocks` 遵守规则。
    - `ActorStatsContext` 提供运行时战斗属性与状态覆盖。
    - `ActorStatsPreviewData` 强类型承载正负预期差值与闪烁效果。
  - **响应式面板组件（`ActorStatsPanel`）**：
    - 公开接口升级为 `bind_actor()`、`set_context()`、`set_preview()`、`clear_preview()`、`refresh()`、`unbind_actor()`。
    - 自动接入 `actor.stats_changed` 信号并在数值变化时自动重绘；更换 Actor 或节点退出树（`_exit_tree`）时安全断开信号。
    - 通过 Godot MCP Pro 为 `actor_stats_panel.tscn` 增补了内置 `AtbRow`（含 `AtbBar` 与 `AtbPreviewSegment`）与 `CpLabel`。
  - **全面迁移消费端 UI**：
    - HUD（`game_stats_column.gd`）接入 `bind_actor(player, hud())`。
    - ESC 菜单（`esc_menu.gd`）接入 `bind_actor(player, menu())`，装备/技能/消耗品悬浮使用 `ActorStatsPreviewData`。
    - 战斗界面（`battle_ui.gd`）分别对 Player 和 Enemy 接入 `bind_actor()`，使用 `ActorStatsContext` 传递 Buff/ATB/有效属性，使用 `ActorStatsPreviewData` 承载 `BattleCalculator` 预览差值，移除旧的每帧轮询。
    - 升级加点界面（`level_up_ui.gd`）接入 `bind_actor(player, progression())`，点数分配变动直接更新 PreviewData。
    - NPC 互动界面（`npc_interaction_ui.gd`）接入 `bind_actor(player, menu())`，强化预览使用 PreviewData。
  - **清理废弃组件与旧入口**：
    - 删除了冗余的 `scenes/ui/components/player_stat_hud.tscn`、`scripts/ui/components/player_stat_hud.gd` 及其 `.uid` 文件。
    - 删除了所有 `_build_player_view()`、`_build_enemy_view()`、`_build_stats_view()` 以及 `ActorStatsViewData.from_player()`。

### 测试结果

通过：基础回归 8/8 项全部通过（BR-001 ~ BR-008 均 PASS，编辑器 0 报错）；主世界 HUD、ESC 菜单背包/技能/装备焦点预览、战斗界面、升级加点与 NPC 界面全部验证正常。

### 证据

- 基础回归报告：`tmp/tests/run-20260817-090635/reports`
- 截图证据：`tmp/tests/run-20260817-090635/screenshots/main_scene.png`
- 核心文件：`scenes/ui/components/actor_stats_panel.tscn`, `scripts/ui/components/actor_stats_panel.gd`, `scripts/ui/components/actor_stats_factory.gd`, `scripts/ui/components/actor_stats_display_profile.gd`, `scripts/ui/components/actor_stats_context.gd`, `scripts/ui/components/actor_stats_preview_data.gd`

### 更新时间

2026-08-17

### 更新内容

- 完成了护盾视觉条（支持超出 Max HP）与全 15 项属性 Delta 扩展：
  - **常态护盾条与超上限渲染（`HpShieldSegment`）**：
    - 通过 Godot MCP Pro 在 `HpBar` 内部增加了专用的常态护盾层 `HpShieldSegment`（青色 `#45AAF2`），并按 `HpTicks` -> `HpShieldSegment` -> `HpPreviewSegment` 分层排布。
    - `ActorStatsViewData` 新增 `get_effective_hp_max()` 动态尺度计算：$\max(\text{preview\_max\_hp}, \text{current\_hp} + \text{current\_shield} + \max(0, \text{current\_shield\_delta}))$，确保护盾超出 Max HP 时血条自动延展比例，护盾段始终紧贴在当前血量右侧。
    - 护盾增减预览支持双层脉冲闪烁（护盾增加闪烁青色，破盾削减闪烁红色）。
  - **全属性 Delta 支持与 Start FP / FP Bar 流转**：
    - `ActorStatsPreviewData` 与 `ActorStatsViewData` 补充了 `max_fp_delta`、`start_fp_delta`、`cp_delta` 等全量 15 项属性的 Delta 容器与更新方法。
    - 确立了 Start FP 生效逻辑：`BattleManager` 战斗初始化时直接从 `start_fp` 开始，面板在 `PROGRESS_BAR` 模式下展示 FP 进度条与消耗预览；在 `RECOVERY_SPEED` 模式下展示 `fp_recovery_spd /s`。

### 测试结果

通过：基础回归 8/8 项全部通过（BR-001 ~ BR-008 均 PASS，编辑器 0 报错）；主世界 HUD、ESC 菜单、战斗及加点场景血条与护盾条渲染正常。

### 证据

- 基础回归报告：`tmp/tests/run-20260817-112140/reports`
- 截图证据：`tmp/tests/run-20260817-112140/screenshots/main_scene.png`
- 核心文件：`scenes/ui/components/actor_stats_panel.tscn`, `scripts/ui/components/actor_stats_panel.gd`, `scripts/ui/components/actor_stats_view_data.gd`, `scripts/ui/components/actor_stats_preview_data.gd`, `scripts/ui/components/actor_stats_factory.gd`

### 更新时间

2026-08-17

### 更新内容

- 完成了 `ActionEffectData` 与 `StatusEffectData` 的状态管理解耦与系统重构：
  - **状态数据定义（`StatusEffectData` Resource）**：
    - 4 大模块变量：
      1. 效果：`affected_stat`（ATK/DEF/SPD/SKILL_POWER/MAX_HP/MAX_MP）、`operation`（ADD/MULTIPLY）、`value`、`restrict_skill_type` 与 `target_skill_type`；
      2. 结束条件：`end_condition`（PERMANENT/OWNER_ACTIONS/SHIELD_DEPLETED/TRIGGER_COUNT）、`duration_count`（为 0 表示常驻永久）、`trigger_type`（ON_ANY_ACTION/ON_PHYSICAL_ATTACK/ON_MAGICAL_ATTACK）；
      3. 显示定义：`display_name`、`display_template`（支持 `{name}`, `{stat_name}`, `{value}`, `{pct}`, `{remaining}`, `{owner_actions}`, `{stacks}` 动态插值）、`icon`；
      4. 极性与策略：`polarity`（BUFF/DEBUFF/NEUTRAL）、`stack_policy`（REFRESH/ADD_STACK/EXTEND/INDEPENDENT/REPLACE）、`max_stacks`。
  - **运行时状态实例（`ActiveStatus` RefCounted）**：
    - 持有状态数据、剩余行动/触发次数、当前层数；
    - 封装 `is_permanent()`、`is_expired()`、`decrement_trigger()`、`decrement_owner_action()`、`get_formatted_text()` 等高频流转操作。
  - **即时动作效果重构（`ActionEffectData` Resource）**：
    - 纯粹即时效果：`target`（SELF/OPPONENT/ALL）、`resource_type`（HP/MP/FP/SHIELD/NONE）、`calc_method`（FIXED_AMOUNT/SKILL_POWER/MAX_RATIO）、`value`、`is_interrupt` 打断标记、`status_to_apply`（施加的状态 Resource）。
  - **单一真值源状态管理器（`BattleStatusController`）**：
    - 统一保存并流转战斗内所有 Actor 的 `ActiveStatus`；
    - 提供属性加成计算 `get_effective_stat()`、技能威力加成 `get_skill_power_modifier()`、状态生命周期钩子（`on_action_finished()`、`on_trigger_event()`、`on_shield_depleted()`）以及供 UI 读取的格式化字典生成。
  - **战斗系统与计算器重构（`BattleManager` / `BattleCalculator` / `GameFormulas`）**：
    - `BattleManager` 开局根据被动技能为 Actor 注册常驻 `ActiveStatus`，行动结束与破盾时驱动 Controller 结算过期；战后统一清理；
    - `BattleCalculator` 与 `GameFormulas` 完全对接 Controller，废弃无类型字典与旧 Effect 结构。
  - **UI 极性色彩与消费端适配**：
    - `ActorStatsPanel` 支持 Buff（绿色 `#55EFC4`）、Debuff（红色 `#FF7675`）、Neutral（浅灰 `#DFE6E9`）极性颜色渲染；
    - 全量更新 `resources/skills/*.tres` 与 `resources/items/*.tres` 资源。
    - 清理了全部废弃的旧属性（`effect_type`、`operation_type`、`duration` 等）。

### 测试结果

通过：全量基础回归 8/8 项全部通过（BR-001 ~ BR-008 均 PASS，退出码 0，编辑器 0 报错，主场景启动、截图、节点树读取与退出均正常）。

### 证据

- 基础回归报告：`tmp/tests/run-20260817-120314/reports`
- 截图证据：`tmp/tests/run-20260817-120314/screenshots/main_scene.png`
- 核心文件：`scripts/data/status_effect_data.gd`, `scripts/battle/active_status.gd`, `scripts/data/action_effect_data.gd`, `scripts/battle/battle_status_controller.gd`, `scripts/battle/battle_manager.gd`, `scripts/battle/battle_calculator.gd`, `scripts/shared/game_formulas.gd`, `scripts/ui/components/actor_stats_panel.gd`

### 更新时间

2026-08-17

### 更新内容

- 完成了全局统一技能说明与详情展示系统（Unified Skill Info System）构建：
  - **规范消耗输出（`ActionCostData`）**：
    - 统一格式为 `消耗生命 %.0f`、`消耗魔力 %.0f`、`消耗专注 %.0f`、`冷却 %.0f 次行动`、`吟唱：行动条倒退 %.0f%%`。
  - **规范即时与状态效果输出（`ActionEffectData`）**：
    - 统一明确目标前缀（`对自身` / `对敌方` / `对全体`）；
    - 伤害与恢复：`造成 %.0f 点技能威力伤害` / `减少 %.0f 点{HP/MP/FP}` / `获得 %.0f 点护盾`；
    - 打断：`打断吟唱`；
    - 状态施加：`施加【{display_name}】：{get_formatted_description()}`。
  - **编号列表与结构化整合（`SkillData`）**：
    - `get_details()` 重构为标准结构化输出：包含 `类别`、可选 `说明`、带编号（`1. `、`2. `）的 `【效果】` 列表以及带编号的 `【消耗】` 列表（无消耗时标明 `1. 无消耗`）。
  - **消费端无缝统一迁移**：
    - `EntryInfoPanel` 新增 `display_skill(skill: SkillData)` 统一展示入口；
    - ESC 菜单（`esc_menu.gd`）与战斗界面（`battle_ui.gd`）统一使用 `entry_info_panel.display_skill(skill)`；
    - NPC 技能导师（`skill_tutor.gd`）对话与详细弹窗全面采用 `SkillData.get_details()`，彻底消除散乱拼装逻辑。

### 测试结果

通过：全量基础回归 8/8 项全部通过（BR-001 ~ BR-008 均 PASS，退出码 0，编辑器 0 报错，主场景启动、截图、节点树读取与退出均正常）。

### 证据

- 基础回归报告：`tmp/tests/run-20260817-124748/reports`
- 截图证据：`tmp/tests/run-20260817-124748/screenshots/main_scene.png`
- 核心文件：`scripts/data/action_cost_data.gd`, `scripts/data/action_effect_data.gd`, `scripts/data/skill_data.gd`, `scripts/ui/components/entry_info_panel.gd`, `scripts/ui/esc_menu.gd`, `scripts/battle/battle_ui.gd`, `scripts/npc/skill_tutor.gd`

### 更新时间

2026-08-17

### 更新内容

- 完成了 NPC 交互通用化与右下角多态详情浮窗重构，并将取消按钮内联为同级别选项：
  - **清理硬编码颜色**：从 `SkillData.gd` 中移除 `[color=#A0A0A0]` BBCode 标签，纯净输出结构化文本，交由 Theme 与控件样式统一定制。
  - **右下角独立悬浮详情面板（`SelectionDetailPanel`）**：
    - 通过 Godot MCP Pro 在 `scenes/ui/npc/npc_interaction_ui.tscn` 的 `InteractionRoot` 挂载独立锚定在右下角的 `EntryInfoPanel`。
    - 在 `NpcInteractionUI.gd` 中实现通用多态展示方法 `show_entry_detail(entry: Variant)`：
      - `SkillData` → 自动调用 `display_skill()` 展示类别、说明、编号效果、编号消耗；
      - `ItemData`（含 `EquipmentData`） → 自动调用 `display_item()` 展示物品图标、描述与装备属性加成；
      - `EntryInfoViewData` → 自动调用 `display_info()` 展示自定义图文信息；
      - `null` 或取消按钮焦点 → 自动隐藏详情浮窗。
  - **取消按钮内联为同级别选项（Inline Option Row）**：
    - 移除了场景中原本孤立在底部的独立 `CancelButton` 节点；
    - 在 `NpcInteractionUI.gd` 的 `_rebuild_rows` 中，将“取消”直接作为最后一个 `SelectableListRow` 动态添加到 `OptionRows` 中，与技能选项拥有完全相同的尺寸、边距、字体、高亮和焦点流转；
    - 对话框内部彻底移除冗长技能/物品说明文本，仅保留简明操作反馈。
  - **SkillTutor 全面对接通用详情与简要反馈**：
    - 导师传授新技能与冲突技能作为 `Resource` 传递，由 `SelectionDetailPanel` 自动高保真渲染。

### 测试结果

通过：全量基础回归 8/8 项全部通过（BR-001 ~ BR-008 均 PASS，退出码 0，编辑器 0 报错，主场景启动、截图、节点树读取与退出均正常）；游戏运行时通过 MCP 验证技能选项、冲突技能与内联取消选项同级别排列，焦点流转与详情浮窗联动完全符合预期。

### 证据

- 基础回归报告：`tmp/tests/run-20260817-140146/reports`
- 截图证据：
  - 主场景基础截图：`tmp/tests/run-20260817-140146/screenshots/main_scene.png`
- 核心文件：`scenes/ui/npc/npc_interaction_ui.tscn`, `scripts/npc/npc_interaction_ui.gd`, `scripts/data/skill_data.gd`, `scripts/npc/skill_tutor.gd`

### 更新时间

2026-08-17

### 更新内容

- 修复 Floor Info HUD 边缘固定间隔与四层楼层信息丢失及跨层回溯问题：
  - **FloorInfoHUD 固定边距重构**：
    - 在 `scenes/ui/game_hud.tscn` 中将 `EnvironmentInfoArea` 锚定于左下角基准点 `(24.0, -24.0)`，`grow_vertical = GROW_DIRECTION_BEGIN`（向上延展）；
    - 在 `scripts/ui/components/environment_info_area.gd` 中设置缩放居中淡入后，平滑回归至基准静止点 `Vector2(0, -info_hud.size.y)`，确保无论描述文本多长，面板距屏幕左边与底边始终保持恒定的 24px 边距；
    - 在 `scripts/ui/components/floor_info_hud.gd` 中加入延迟自适应重绘 `_update_resting_position()`，确保在场景恢复与事件驱动下均能保持静止对齐。
  - **四层出生点缺失与跨层回溯修复**：
    - 通过 Godot MCP Pro 在 `scenes/floors/floor_4.tscn` 中补充了缺失的 `FromAboveStair` Marker2D（坐标 `(0, -672)`），并设置 `default_spawn_id = &"FromBelowStair"`；
    - 在 `scripts/floors/floor.gd` 的 `get_spawn_point()` 中增加了多层级安全回退机制（指定点 -> 默认出生点 -> 容器首个 Marker2D），防止因出生点缺失导致楼层切换失败并清空 HUD 信息。

### 测试结果

通过：全量基础回归 8/8 项全部通过（BR-001 ~ BR-008 均 PASS，退出码 0，编辑器 0 报错，主场景启动、截图、节点树读取与退出均正常）；实测从 Floor 4 到 Floor 5 再回到 Floor 4，楼层标题、描述与固定 24px 边距全部正常加载并展示。

### 证据

- 基础回归报告：`tmp/tests/run-20260817-141026/reports`
- 截图证据：
  - 主场景基础截图：`tmp/tests/run-20260817-141026/screenshots/main_scene.png`
- 核心文件：`scenes/floors/floor_4.tscn`, `scenes/ui/game_hud.tscn`, `scripts/ui/components/environment_info_area.gd`, `scripts/ui/components/floor_info_hud.gd`, `scripts/floors/floor.gd`

### 更新时间

2026-08-17

### 更新内容

- 重构 `ActorStatsPanel` 与数据工厂，实现全局统一的本质化多态 Stat Preview 与自动清零机制：
  - **统一多态工厂（`ActorStatsFactory`）**：
    - 新增 `create_preview_from_equipment()`、`create_preview_from_item()`、`create_preview_from_skill()`、`create_preview_from_permanent_increase()` 与通配工厂 `create_preview_from_entry()`；
    - `create_view_data()` 严格保证只有当 `preview != null and preview.has_any_preview()` 时才附加 Delta，否则所有 15 项属性 Delta 全量归 0。
  - **本质化 Stat Panel 预览接入（`ActorStatsPanel.set_preview()`）**：
    - `set_preview(preview: Variant)` 升级为多态接收器：
      - 传入 `EquipmentData` / `ItemData`（消耗药水等）/ `SkillData`（增益技能）时，自动通过工厂计算差值并渲染；
      - 传入 `ActorStatsPreviewData` 时，若各项 Delta 为 0（`has_any_preview() == false`）自动识别为无预览；
      - 传入 `null`、非属性物品（如石头、任务道具）、取消按钮、普通对话选项或任何不改变属性的对象时，自动将 `_preview` 置为 `null`，确保全属性 Delta **无条件归 0**。
    - 新增 `preview_permanent_increase(stat_id, amount)`，统一加点与永久属性增量预览。
  - **全面精简各消费端 UI（ESC 菜单、NPC 交互界面）**：
    - `esc_menu.gd`：`_on_item_focused` 与 `_on_skill_focused` 直接接入 `actor_stats_panel.set_preview(item/skill)`，移除重复的药水与技能效果解析代码；
    - `npc_interaction_ui.gd`：`show_entry_detail(entry)` 统一接入 `stats_panel.set_preview(entry)`，无论是焦点移动、取消选项、物品切换还是关闭对话，只要不是改变属性的对象，面板 Delta 均在底层自动归 0。

### 测试结果

通过：全量基础回归 8/8 项全部通过（BR-001 ~ BR-008 均 PASS，退出码 0，编辑器 0 报错，主场景启动、截图、节点树读取与退出均正常）；游戏运行时通过 MCP 验证：
1. 装备（`chest_lv2`）传入 `set_preview` -> 正常显示 `DEF (+20)`；
2. 消耗品（`hp_recovery_lv1`）传入 `set_preview` -> 正常显示 `HP (+20)`；
3. 非属性物品（`stone`）传入 `set_preview` -> 属性 Delta 自动为 0（无高亮与加号）；
4. `null` / `"取消"` / 选项聚焦切换传入 `set_preview` -> 属性 Delta 自动为 0；
5. 碎片共鸣者永久属性提升与“取消”焦点流转时差值自动响应与归 0。

### 证据

- 基础回归报告：`tmp/tests/run-20260817-164048/reports`
- 截图证据：
  - 主场景基础截图：`tmp/tests/run-20260817-164048/screenshots/main_scene.png`
- 核心文件：`scripts/ui/components/actor_stats_factory.gd`, `scripts/ui/components/actor_stats_panel.gd`, `scripts/ui/esc_menu.gd`, `scripts/npc/npc_interaction_ui.gd`, `scripts/npc/fragment_exchanger.gd`

### 更新时间

2026-08-17

### 更新内容

- 完成了全局技能冷却（Cooldown）纯 `int` 强类型化与全流程重构：
  - **数据层源头规范化（`SkillData` / `ActionCostData`）**：
    - `SkillData` 直接在 Inspector 暴露 `@export_range(0, 99, 1) var cooldown: int = 0`，原生定义冷却行动数；
    - `SkillData.get_cooldown() -> int` 统一返回整数冷却回合；
    - `ActionCostData.get_cooldown() -> int` 辅助函数统一解析并输出 `%d 次行动`；
    - `SkillData.get_details()` 自动呈现结构化整数 `冷却：%d 次行动`。
  - **运行期字典与计算类型收敛（`BattleManager` / `BattleEnemyAI` / `BattleUI` / `SkillPanel`）**：
    - `BattleManager._cooldowns` 与 `_enemy_cooldowns` 声明为 `Dictionary[StringName, int]`；
    - `get_skill_cooldown(skill_id, is_enemy) -> int` 强类型返回整数；
    - `_decrement_cooldowns` 纯整型递减，彻底消除运行期 `int(...)` 强转；
    - `SkillPanel` 强类型接收 `cd: int` 渲染 `(CD: %d)`。
  - **全量技能资源标准化**：
    - 更新 `kick_off.tres`（4）、`heal.tres`（5）、`overflow.tres`（1）、`overload_atk.tres`（8），将 cooldown 提升至 `SkillData` 原生属性，移除冗余的消耗子资源。

### 测试结果

### 更新时间

2026-08-17

### 更新内容

- 完成了战斗界面怪物面板两段式独立框重构与施法强类型数组安全防护：
  - **施法与技能结算 `Array[Node]` 强类型安全防护（`BattleManager` & `BattleCalculator`）**：
    - 彻底消除了 `BattleManager._apply_preview`、`BattleCalculator.evaluate_skill_effects` 以及 `BattleCalculator.evaluate_item` 中对字面量未类型化数组的直接赋值（`targets = [_player, _enemy]` / `effect_targets = [caster]` 等），改为显式构造强类型 `var effect_targets: Array[Node] = []` 并逐一安全 `append`；
    - 修复了 `BattleManager._apply_preview` 中由于多态三元表达式（`Player` 与 `Enemy` 静态类型推断）引起的 GDScript 编译报错，消除了施放增益/群体技能（如“战斗怒吼”）时的运行时类型异常。
  - **怪物右侧两段式独立面板重构（`EnemyStats` 上框 + `EnemyForecastPanel` 下框）**：
    - **上框（`EnemyStats` / `ActorStatsPanel`）**：
      - `ActorStatsDisplayProfile.battle_enemy()` 开启 `show_buffs = true`，在上框集中展示怪物核心数值（HP/MP/FP/ATK/DEF/SPD 等）以及怪物当前生效的 **Buff / 被动介绍**；
      - `BuffLabel`（`scenes/ui/components/buff_label.tscn`）升级为使用 `theme_type_variation = &"BuffLabel"`，统一采用 Theme 中的 20px 字体与智能换行（`AUTOWRAP_WORD_SMART`），在 `ActorStatsPanel._refresh_buffs` 中以编号列表（`1. 【刺甲】...`）与极性颜色高亮呈现；
    - **下框（`EnemyForecastPanel`）**：
      - 专心展示**怪物将要使用的技能**与**技能介绍**（技能名称、图标、类别、编号效果与编号消耗详情）。

### 测试结果

通过：全量基础回归 8/8 项全部通过（BR-001 ~ BR-008 均 PASS，退出码 0，编辑器 0 报错，主场景启动、截图、节点树读取与退出均正常）；实测“战斗怒吼”施放与 Buff 状态解析生效，怪物右侧上下面板结构分明、样式统一。

### 证据

- 基础回归报告：`tmp/tests/run-20260817-211846/reports`
- 截图证据：
  - 主场景基础截图：`tmp/tests/run-20260817-211846/screenshots/main_scene.png`
- 核心文件：`scripts/battle/battle_manager.gd`, `scripts/battle/battle_calculator.gd`, `scripts/battle/battle_ui.gd`, `scenes/ui/components/buff_label.tscn`, `resources/themes/game_ui_theme.tres`, `scripts/ui/components/actor_stats_display_profile.gd`, `scripts/ui/components/actor_stats_panel.gd`, `scripts/ui/components/enemy_forecast_panel.gd`

### 更新时间

2026-08-18

### 更新内容

- 完成了从 Floor 1 到 Floor 6 的多轮端到端全流程通关游玩测试与场景数据修复：
  - **修复 Floor 6 场景根节点 `floor_id` 缺失导致的跨层阻断 Bug**：
    - `scenes/floors/floor_6.tscn` 此前未重写 `floor_id` 属性（默认继承为空），导致 `FloorManager._instantiate_floor` 在 ID 校验时匹配失败并销毁；
    - 通过 Godot MCP Pro 对 `floor_6.tscn` 补全了 `floor_id = "floor_6"`、`display_name = "六层：通关"`、`description = "恭喜你！成功登上了第一座高塔的顶层！"` 以及 `default_spawn_id = "FromBelowStair"`，打通了最终通关路径。
  - **三主流派多轮端到端真实通关游玩验证（Run 1: 力量流 / Run 2: 极速流 / Run 3: 坚韧流）**：
    - **合法行走路径约束**：Floor 1 ~ Floor 5 全程严格仅行走在有效地面（Ground）、交互机关（Switch/Stair/Tutor）与怪物网格上，未涉足任何空白虚空区域；
    - **Floor 1（三翼教学）**：初始开关破墙，通关红/蓝/黄三翼战斗，收集手/脚/胸装备，全部机关激活后开启中央楼梯顺利进入 Floor 2；
    - **Floor 2（专注与兑换）**：碎片兑换 NPC 交互、小怪清理、头部装备收集与机关开启，顺利进入 Floor 3；
    - **Floor 3（规则迷宫）**：严格遵循“只能直行与左转”的行进约束，完成战斗并触发 `Switch1`，顺利进入 Floor 4；
    - **Floor 4（Boss 战）**：沿途拾取 8 瓶高阶治疗药水，在迎战攻击之王 Boss 时实测在 Boss 吟唱“湮灭”期间精准使用瞬发打断技能“踢击”（`kick_off`）瓦解毁灭打击，配合药水与输出成功斩杀 Boss 挺进 Floor 5；
    - **Floor 5（抉择与复合单向门网络）**：向导师学习对应专属过载大招（`overload_atk`/`overload_def`/`overload_spd`），操控单向门换向开关，成功激活 3 座中立核心机关（`SwitchNeu1`、`SwitchNeu2`、`SwitchNeu3`），中央通道与终点楼梯（`StairAbove`）成功显现并解锁；
    - **Floor 6（通关殿堂）**：各流派均成功登上 Floor 6，底部 HUD 正确呈现通关提示。

### 测试结果

通过：基础回归 8/8 项全部 PASS（0 报错退出），三轮多流派端到端模拟游玩均 100% 顺利通关抵达 Floor 6，全楼层机关与战斗逻辑闭环验证完毕。

### 证据

- 基础回归报告：`tmp/tests/run-20260818-092338/reports`
- 截图证据：
  - Floor 1 初始截图：通过 Godot MCP Pro 捕获
  - Floor 2 专注层截图：通过 Godot MCP Pro 捕获
  - Floor 3 规则迷宫截图：通过 Godot MCP Pro 捕获
  - Floor 4 Boss 战与打断提示截图：通过 Godot MCP Pro 捕获
  - Floor 5 三机关激活开门截图：通过 Godot MCP Pro 捕获
  - Floor 6 通关登顶截图：通过 Godot MCP Pro 捕获
- 核心文件：`scenes/floors/floor_6.tscn`, `tools/test_full_game_playthrough.gd`, `scripts/floors/floor_5.gd`

### 更新时间

2026-08-18 11:15 EDT

### 更新内容

- 统一 `DialogueNpc` 的贴图来源：根节点 `texture` 作为唯一配置，子节点 `Sprite2D` 不再保存独立旧贴图。
- 修复 `DialogueNpc` 的编辑器导出属性 setter，使用显式 backing 变量同步 Visuals 到子节点。
- 清理脚本缩进错误，确保 `@tool` 脚本可以被编辑器加载并显示 `Visuals`、`Identity`、`Dialogue` 分组。

### 测试结果

- **通过**：Floor 5 的 `DialogueNpc` 根节点与 `Sprite2D` 均解析为 `res://assets/interactables/statue_atk.png`。
- **通过**：`python3 tools/run_basic_regression.py`，8/8 通过，0 失败，编辑器错误 0。

### 更新时间

2026-08-18 15:58 EDT

### 更新内容

- 完成了战斗界面（Battle UI）技能预览与 Buff 的完全对称布局重构：
  - **左右双塔对称结构构建**：
    - **左栏（`LeftColumn`）**：上层为玩家属性面板（`PlayerStats`，包含属性及 Buff 列表），下层挂载 `PlayerActionInfo`（直接复用 `EntryInfoPanel`），用于在玩家浏览/聚焦技能或道具时即时呈现效果详情。
    - **中栏（`CenterColumn`）**：保留顶部行动进度条（`SharedAtb`）与分页标签栏（`ActionTabs`），移除原内置的右半侧详情面板，使技能列表（`SkillPanel`）、道具列表（`InventoryPanel`）和撤退界面（`EscapePage`）占据中栏完整宽度，操作空间大幅扩展。
    - **右栏（`RightColumn`）**：上层为敌人属性面板（`EnemyStats`，包含属性及 Buff 列表），下层挂载 `EnemyActionInfo`（直接复用 `EntryInfoPanel`），用于呈现敌方 AI 决策选定的即将释放技能预告（或普攻说明）。
  - **统一条目详情模板与接口拓展（`EntryInfoPanel`）**：
    - `EntryInfoPanel` 增加 `fallback_to_basic_attack` 支持，当敌方 AI 决策为普通攻击时统一呈现标准的物理类别与基础效果。
    - 淘汰并删除了冗余的过渡包装场景与脚本 `scenes/ui/components/enemy_forecast_panel.tscn`、`scripts/ui/components/enemy_forecast_panel.gd`。
  - **精简无消耗技能与普攻展示（`SkillData.get_details()`）**：
    - 修改 `SkillData.get_details()`，在技能没有资源消耗或 CD 时彻底隐藏 `【消耗】` 区块，只呈现核心 `【效果】` 说明，消除 `1. 无消耗` 的视觉冗余。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：运行时战斗验证通过，左右两翼 `EntryInfoPanel` 正常驱动并对称呈现，无消耗技能仅显示效果区块，有消耗技能正常显示消耗明细。

### 更新时间

2026-08-18 17:21 EDT

### 更新内容

- 完成了玩家移动锁的去中心化解耦重构（Decentralized Movement Lock Tokens）：
  1. **命名锁机制（`PlayerMovement` / `Player`）**：
     - 彻底废除 `PlayerMovement.can_move_in_world()` 对外部 UI 场景的反向探测；
     - 引入命名锁管理器：`lock_movement(lock_id: StringName)`、`unlock_movement(lock_id: StringName)`、`is_movement_locked() -> bool`、`clear_all_movement_locks()`；
     - 支持任意多个业务系统同时加锁，当且仅当所有锁被全部释放时，玩家方可在大地图上移动。
  2. **业务系统自闭环锁定与解锁**：
     - **战斗系统（`BattleManager`）**：进入战斗调用 `_player.lock_movement(&"battle")`，战斗结束调用 `_player.unlock_movement(&"battle")`；
     - **ESC 菜单（`EscMenu`）**：菜单打开调用 `_player.lock_movement(&"esc_menu")`，菜单关闭调用 `_player.unlock_movement(&"esc_menu")`；
     - **NPC 对话（`NpcInteractionManager`）**：对话开启调用 `_player.lock_movement(&"npc_interaction")`，对话结束调用 `_player.unlock_movement(&"npc_interaction")`；
     - **教程弹窗（`TutorialManager`）**：教程弹窗出现调用 `_player.lock_movement(&"tutorial")`，教程完成调用 `_player.unlock_movement(&"tutorial")`；
     - **升级加点（`LevelUpManager`）**：加点打开调用 `_player.lock_movement(&"level_up")`，加点确认调用 `_player.unlock_movement(&"level_up")`；
     - **开场与过场（`Main` / `IntroVideo`）**：开场调用 `player.lock_movement(&"intro_video")`，结束调用 `player.unlock_movement(&"intro_video")`；
     - **楼层与传送（`FloorManager` / `Portal` / `OneWayPassage`）**：传送开始锁定对应锁标识，传送结束解锁；
     - **存档载入（`SaveManager`）**：读档成功后调用 `_player.clear_all_movement_locks()` 重置为纯净探索状态。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：10 项全场景命名锁生命周期与重叠嵌套测试全部通过断言：
  1. 大地图初始探索状态：`is_locked == false`；
  2. ESC 菜单开启：`is_locked == true`；
  3. ESC 菜单关闭：`is_locked == false`；
  4. 战斗开启：`is_locked == true`；
  5. 战斗中触发教程（2把锁）：`is_locked == true`；
  6. 教程关闭解除教程锁（仍有战斗锁）：`is_locked == true`；
  7. 战斗结束解除战斗锁：`is_locked == false`；
  8. 升级加点开启：`is_locked == true`；
  9. 升级加点关闭：`is_locked == false`；
  10. 存档并读档：`is_locked == false`。

### 更新时间

2026-08-18 17:26 EDT

### 更新内容

- 修复了存档界面（`SystemPanel`）删除存档后焦点丢失（Focus Loss）的体验问题：
  1. **指定位置焦点导航（`FocusableList`）**：
     - 在 `scripts/ui/components/focusable_list.gd` 中新增 `focus_row_at(index: int, fallback_button_index: int = 0) -> bool` 方法，支持将焦点精确投递到指定行索引的对应操作按钮（读取 / 覆盖 / 删除）。
  2. **焦点状态记忆与平滑恢复（`SystemPanel`）**：
     - 在 `scripts/ui/components/system_panel.gd` 的 `refresh_saves()` 过程中，捕获刷新前的焦点行号（`focused_row_index`）和按钮类型（`focused_button_index`）；
     - 当某行存档被删除并刷新列表时：
       - 若列表中仍有存档：自动将焦点重定向至紧邻行（`clampi(focused_row_index, 0, save_rows.get_rows().size() - 1)`）的同类操作按钮（例如仍留在“删除”按钮上，便于玩家连续管理）；
       - 若所有存档均已被删空：平滑将焦点退回至顶部的 `create_button`（创建存档按钮），彻底杜绝焦点丢失为 null 的现象。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：实机验证：
  - 删除任意一条非最后一条存档时，焦点平滑保留在后续存档行的删除按钮上；
  - 删除最后一条存档导致列表清空时，焦点平滑回退到创建按钮上，方向键导航依然顺畅。

### 更新时间

2026-08-18 17:28 EDT

### 更新内容

- 修复了玩家按一下方向键导致角色无限连续移动（而非单步移动）的严重输入缺陷：
  1. **按键释放事件阻断修复（`PlayerMovement`）**：
     - 此前在 `PlayerMovement._unhandled_input` 头部使用了 `is_moving` 判断直接 `return`，导致玩家按下方向键触发单格移动后、迅速松开按键产生的 `event.is_released()` 事件被全部丢弃；
     - 导致 `_held_directions` 中永远残留该移动方向，单格移动结束后误判定为“玩家仍在按住方向键”而无限滑行。
  2. **输入事件与持续按住双重保障**：
     - 移除了 `_unhandled_input` 头部用 `is_moving` 对松键事件的阻断，保证 `_forget_held_direction` 必定被执行；
     - 重构 `_get_active_held_direction()`，在单格位移结束时通过 `Input.is_action_pressed()` 与 `_held_directions` 严谨校验当前是否真的被物理按住；
     - 使得“轻点（Tap）方向键”精准行走 1 格并立即停止，“长按（Hold）方向键”流畅连贯走多格。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：实机模拟 Tap 输入测试，按下并松开后，队列立即清空，单格位移结束后 `is_moving` 准确置为 `false`，未产生多余位移。

### 证据

- 基础回归报告：`tmp/tests/run-20260818-182822/reports/`
- 截图证据：`tmp/tests/run-20260818-182822/screenshots/main_scene.png`
- 修改文件：
  - `scripts/actors/components/player_movement.gd`

### 更新时间

2026-08-18 18:30 EDT

### 更新内容

- 完成了货币系统的科幻化重构（Gold $\to$ Credit 信用点）：
  1. **实体物品与材料化（`credit.tres`）**：
     - 新建 `resources/items/credit.tres`（`ItemData`），归类为 `ItemType.MATERIAL`（材料），使用通用算力 Token 图标（`universal_token_tier_1.png`）。
     - 支持作为普通 `item_pickup.tscn` 放在地图上供玩家直接拾取。
     - 在 ESC 菜单 -> 背包 -> 材料栏 中自动陈列信用点及其實时数量。
  2. **怪物掉落与 CP 动态绑定**：
     - 怪物掉落信用点默认严格等于其 $\text{CP} = \text{roundi}\left(\frac{ATK + DEF + SPD}{10}\right)$，亦支持在 `EnemyData` 中覆盖。
  3. **战后结算双重反馈与提示**：
     - 战后结算时统一向玩家背包发放 Credit 物品，同时广播提示：`"战斗胜利！获得了 %d 点经验值、%d 点信用点。"`。
  4. **HUD 实时追踪（`TrackedInventoryHUD`）**：
     - 在 `scenes/ui/components/tracked_inventory_hud.tscn` 中挂载 Credit 追踪行，持有数量 $> 0$ 时实时呈现在游戏主界面左上方。
  5. **单一真值源管理**：
     - 废除 `PlayerProgression.gold`，`player.credits` 直接代理查询 `inventory.get_quantity(&"credit")`，消除状态不同步。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：Credit 专属单元与集成测试（`tests/test_credit_system.gd`）：
  - `credit.tres` 资源属性校验通过；
  - 1级攻击怪（CP=12）、1级防御怪（CP=20）、1阶Boss（CP=27）掉落计算通过；
  - 战后背包自动增加入库，系统提示包含双重收益播报；
  - HUD 实时更新并展示 `信用点 ×数量`。

### 更新时间

2026-08-18 18:45 EDT

### 更新内容

- 统一经验值（EXP）与信用点（Credit）计算架构至 `GameFormulas`：
  1. **全局公式集中化（`GameFormulas`）**：
     - 在 `GameFormulas` 中定义 `CP_TO_EXPERIENCE_MULTIPLIER: float = 1.0` 与 `CP_TO_CREDIT_MULTIPLIER: float = 1.0`，方便集中调节 CP 转化系数；
     - 新增 `GameFormulas.default_enemy_credit(atk, def, spd) -> int` 方法，与 `default_enemy_experience` 完全对称。
  2. **数据类解耦（`EnemyData`）**：
     - `EnemyData` 恢复为纯数据资源载体（仅保留 `@export var experience_reward_override` 与 `credit_reward_override`），移除实例方法 `get_credit_reward()` 与公式库强依赖。
  3. **战斗管理器对称结算（`BattleManager`）**：
     - `BattleManager._get_credit_reward()` 逻辑与 `_get_experience_reward()` 完全一致：优先读取怪物重写属性，未配置时统一委托 `FORMULAS.default_enemy_credit(...)`。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：`tests/test_credit_system.gd` 测试全部 PASS。

### 证据

- 基础回归报告：`tmp/tests/run-20260818-184407/reports/`
- 截图证据：`tmp/tests/run-20260818-184407/screenshots/main_scene.png`
- 修改文件：
  - `scripts/shared/game_formulas.gd`
  - `scripts/data/enemy_data.gd`
  - `scripts/battle/battle_manager.gd`
  - `tests/test_credit_system.gd`

### 更新时间

2026-08-19 10:43 EDT

### 更新内容

- 彻底精简 `BattleUI.set_action_available`：
  1. **删除多余逻辑**：彻底移除 `set_action_available` 中对 `call_deferred(&"_focus_current_page")` 的调用及其附加的辅助判定；
  2. **极简设计**：`set_action_available` 仅更新 `_action_available = available` 标识；
  3. **交互规范**：战斗开始时（`open()`）焦点置于第 1 个技能，之后全流程完全交由玩家自由移动光标；读条中仅做确认键拦截，读条结束绝不触发任何焦点夺取。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 更新时间

2026-08-19 11:06 EDT

### 更新内容

- 修复了战斗中敌人行动/属性刷新导致玩家当前聚焦技能的预估伤害预览消失的缺陷：
  1. **当前聚焦项记忆（`BattleUI`）**：
     - 在 `scripts/battle/battle_ui.gd` 中维护 `_current_focused_skill: SkillData` 与 `_current_focused_item: ItemData` 引用；
     - 在 `_on_skill_focused` 与 `_on_item_focused` 中实时同步玩家当前选中的技能或道具；
  2. **属性更新后自动重算并恢复预览**：
     - 在 `refresh_stats()` 中刷新完基础属性 Context 之后，自动根据当前页面与聚焦项重新执行 `_on_skill_focused` / `_on_item_focused`；
     - 使得不论敌人何时释放技能、受到伤害或增减 Buff，玩家面板上的技能预估伤害与属性 Delta **不仅绝不丢失，而且依据双方最新的实时属性重新精确计算**；
  3. **页面与生命周期闭环**：
     - 在切换至逃跑页（`ActionPage.ESCAPE`）或战斗关闭（`close()`）时安全重置聚焦引用并清空 Delta 预览。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：实机验证敌人行动并更新属性后，玩家所选技能的伤害 Delta 与 ATB 预测条 100% 完整保留并根据最新属性实时更新。

### 更新时间

2026-08-19 11:31 EDT

### 更新内容

- 完成了战斗预览状态架构的统一重构（Unified Battle Preview State Architecture）：
  1. **单一预览真值源（`BattleUI`）**：
     - 使用统一的多态变量 `_current_preview_entry: Resource` 替代分散的各类型预览变量，支持 `SkillData` 与 `ItemData`；
  2. **刷新管线彻底解耦**：
     - `refresh_base_battle_ui()`：纯粹刷新真实世界状态（HP/MP/FP、Buff、真实 ATB、技能冷却），**绝对不主动清除或污染预览层**；
     - `apply_current_preview()`：根据 `_current_preview_entry` 动态分发重算伤害/消耗/ATB 预测并覆盖渲染到 StatPanel 和 EntryInfoPanel；
     - `set_preview_entry(entry)` / `clear_preview_entry()`：标准化预览设置与清空入口；
     - `refresh_stats()`：统一调度基础刷新与当前预览重算；
  3. **交互规范与生命周期**：
     - 技能/物品聚焦直接接入 `set_preview_entry`；焦点离开/切换逃跑页自动 `clear_preview_entry`；
     - 敌人行动导致属性/Buff 改变时，真实状态刷新与预览重算完美同步。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：实机验证 `_current_preview_entry` 状态流转、`refresh_base_battle_ui` 隔离性与 Escape 清理测试 100% PASS。

### 证据

- 基础回归报告：`tmp/tests/run-20260819-113114/reports/`
- 截图证据：`tmp/tests/run-20260819-113114/screenshots/main_scene.png`
- 修改文件：
  - `scripts/battle/battle_ui.gd`

### 更新时间

2026-08-19 11:55 EDT

### 更新内容

- 修复了游戏启动时直接弹出通关结算界面（GameClearUI）的场景嵌套异常：
  1. **场景树多余展开节点清理（`main.tscn`）**：
     - 在 `scenes/main.tscn` 中，此前打包场景 `GameClearUI` 下意外嵌套并展开了多余的 `ClearRoot2` 树状子节点；
     - 导致 `scripts/ui/game_clear_ui.gd` 在 `_ready()` 中仅隐藏了原版的 `$ClearRoot`，而未隐藏意外泄露展开的 `ClearRoot2`，造成游戏一启动就呈现通关画面的假象；
  2. **通过 Godot MCP Pro 净化场景树**：
     - 使用 `delete_node` 删除了 `main.tscn` 中挂载在 `GameClearUI` 下的多余重名展开节点，使其实例化保持纯净。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：实机验证主场景启动时 `GameClearUI.ui_root.visible == false`，游戏画面与大地图完全正常呈现。

### 更新时间

2026-08-19 12:10 EDT

### 更新内容

- 实现了 UI 打开瞬态防按键连发与长按穿透保护（UI Input Debounce Window & Event Flush）：
  1. **事件队列即时冲刷（`Input.flush_buffered_events()`）**：
     - 在 `BattleUI`、`EscMenu`、`NpcInteractionUI`、`LevelUpUI`、`TutorialUI` 等核心界面的 `open()` / `show_prompt()` 瞬间执行事件冲刷，清空操作系统缓冲区中积压的按键事件；
  2. **180ms 安全防连发窗口（`_input_ready_time`）**：
     - 在 UI 打开的首个 180 毫秒内，丢弃所有方向键连发，确保玩家在大地图上长按方向键撞怪进战斗或按快捷键打开菜单时，光标稳稳停留在默认第 1 项，绝不发生误触连击滚动；
  3. **顺畅恢复交互**：
     - 180ms 窗口期结束后，玩家松手再按方向键即可灵敏平滑地导航，兼顾防误触与操作手感。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：实机模拟长按方向键进入战斗与打开菜单，验证 debounce 窗口期内输入被精准消费拦截，界面选项保持在第一项。

### 证据

### 更新时间

2026-08-19 12:22 EDT

### 更新内容

- 实现了通用按键释放门禁状态机（UIInputGate Release-To-Activate Mechanism）：
  1. **新建通用门禁组件（`scripts/ui/components/ui_input_gate.gd`）**：
     - `reset_gate()`：在 UI 打开（`open()`）时捕获并标记当前处于物理按下状态的所有导航/操作动作（如 `ui_up`, `move_up`, `ui_left` 等）为 `blocked`；
     - `filter_event(event)`：在 UI 输入处理头部进行状态机过滤：
       - 当捕获到对应动作的松开事件（`Release`）时，自动从 `blocked` 集合中解除解锁；
       - 当捕获到对应动作的按下事件（`Pressed`）时，若仍在 `blocked` 集合中则完全阻断丢弃，只有在解锁后产生的新点按动作才放行；
  2. **跨 UI 统一集成**：
     - 全面接入 `BattleUI`、`EscMenu`、`NpcInteractionUI`、`LevelUpUI`、`TutorialUI`；
     - 彻底解决了大地图持续长按方向键撞怪进战斗或开菜单时导致焦点持续滚动的顽疾。

### 测试结果

- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。
- **通过**：实机模拟长按方向键进战斗与开菜单状态机测试（持续长按被 100% 阻断 $\to$ 松开按键 $\to$ 重新按下正常导航）100% PASS。

### 更新时间

2026-08-19 13:16 EDT

### 更新内容

- 实现了补给商店 NPC 系统（ShopNpc / Supply Vendor）与数据驱动的自定义商品定价：
  1. **商店 NPC 逻辑（`scripts/npc/shop_npc.gd`）**：
     - 支持在 Inspector 中可视化配置出售商品列表（`shop_items: Array[ItemData]`）与独立价格（`item_prices: Array[int]`）；
     - 默认配置 25 Credits 兑换 `1级生命药剂`（`hp_recovery_lv1.tres`）与 `1级数据恢复`（`mp_recovery_lv1.tres`）；
     - 支持余额不足拦截、扣除 Credits、发放道具与交易状态实时更新；
  2. **选项行两端对齐与价格标签（`SelectableListRow`）**：
     - 在 `scenes/ui/components/selectable_list_row.tscn` 中挂载高亮金色右对齐 `TrailingLabel`；
     - `setup()` 方法支持传入 `trailing_text`，实现左侧物品名称/图标、右侧价格（如 `25 Credits`）的整齐排版；
  3. **持有量实时联动（`NpcInteractionUI`）**：
     - 在 NPC 交互面板下方新增 `HeldQuantityLabel`，当焦点移至某商品时实时显示 `【背包已持有】 %d 件`；
  4. **消耗品效果说明增强（`EntryInfoPanel`）**：
     - `display_item()` 支持自动解析 `effects` 数组，在右侧详情框直观呈现生命/法力回复量；
  5. **场景实体定义（`scenes/actors/npcs/shop_npc.tscn`）**：
     - 在 IDE 中构建了独立的实体场景，符合项目 StaticBody2D 碰撞层规范。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_shop_npc.gd`）：选项排版、价格展示、持有量联动、不足拦截、购买成功扣除与发放、Inspector 自定义定价调参 100% PASS。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260819-131613/reports/`
- 截图证据：`tmp/tests/run-20260819-131613/screenshots/main_scene.png`
- 修改文件：
  - `scripts/npc/shop_npc.gd` (新建)
  - `scenes/actors/npcs/shop_npc.tscn` (新建)
  - `tests/test_shop_npc.gd` (新建)
  - `scenes/ui/components/selectable_list_row.tscn`
  - `scripts/ui/components/selectable_list_row.gd`
  - `scenes/ui/npc/npc_interaction_ui.tscn`
  - `scripts/npc/npc_interaction_ui.gd`
  - `scripts/ui/components/entry_info_panel.gd`

### 更新时间

2026-08-19 14:07 EDT

### 更新内容

- 完成了 NPC 交互选项文本与持有量排版的极简化重构：
  1. **选项文本紧凑化（Compact Option Text）**：
     - 将持有量直接融入选项左侧主文本：`[图标] 1级红色碎片 [持有数: 2]` / `[图标] 1级生命药剂 [持有数: 0]`；
     - 右侧 Trailing 统一展示收益/成本：`攻击力 +1` / `25 Credits`；
  2. **NPC 交互 UI 架构净化**：
     - 从 `scenes/ui/npc/npc_interaction_ui.tscn` 与 `scripts/npc/npc_interaction_ui.gd` 中移除了多余的 `HeldQuantityLabel` 节点与状态监听，恢复轻量纯粹；
  3. **FragmentExchanger 与 ShopNpc 统一**：
     - `FragmentExchanger` 与 `ShopNpc` 采用完全一致的选项文本构造与 Trailing 传参规范。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_shop_npc.gd`）：测试选项排版、持有数格式、购买状态更新、碎片共鸣者数据流 100% 全部 PASS。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 更新时间

2026-08-19 14:14 EDT

### 更新内容

- 优化了选项行右侧 TrailingLabel 样式与 FragmentExchanger 属性文案：
  1. **字体与样式统一（TrailingLabel）**：
     - 移除了 `scenes/ui/components/selectable_list_row.tscn` 中 `TrailingLabel` 绑定的 `HeaderLabel` 变体与颜色覆盖；
     - 使其完全继承主题默认的 Label 字体与字号，与左侧选项文字视觉效果完全协调一致；
  2. **属性简称精简（FragmentExchanger）**：
     - 将 `scripts/npc/fragment_exchanger.gd` 中的属性名称由“攻击力/防御力/速度”精简为“攻击/防御/速度”；
     - 兑换选项右侧清晰显示为 `攻击 +1`、`防御 +1`、`速度 +1`。

### 测试结果

- **通过**：实机选项渲染与数据流验证：TrailingLabel 字体完全一致，选项右侧文本显示为 `攻击 +1` / `防御 +1` / `速度 +1`，100% PASS。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260819-141329/reports/`
- 截图证据：`tmp/tests/run-20260819-141329/screenshots/main_scene.png`
- 修改文件：
  - `scenes/ui/components/selectable_list_row.tscn`
  - `scripts/npc/fragment_exchanger.gd`

### 更新时间

2026-08-19 20:35 EDT

### 更新内容

- 修复了 HUD 界面中等级（Lv）与经验（Exp）未显示的问题：
  1. **默认解锁（`FeatureUnlockState`）**：
     - 在 `scripts/autoload/feature_unlock_state.gd` 中将 `level` 和 `exp` 设为初始默认解锁（`_init` 及 `restore_save_data` 中默认包含 `&"level": true, &"exp": true`），确保玩家进入游戏及 HUD/菜单加载时即可正常展示等级与经验。
  2. **新手战斗引导保障（`BattleIntroTutorial`）**：
     - 在 `scripts/tutorial/tutorials/battle_intro_tutorial.gd` 的战斗介绍完成阶段（`Step.ATB`）防御性地触发 `unlock(&"level")` 与 `unlock(&"exp")`。

### 测试结果

- **通过**：运行时 MCP 属性断言验证：HUD 属性面板中 `level_label.visible = true`（文本为 `等级 1`），`experience_row.visible = true`（文本为 `0/60`），完全正常渲染。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260819-203427/reports/`
- 截图证据：`tmp/tests/run-20260819-203427/screenshots/main_scene.png`
- 修改文件：
  - `scripts/autoload/feature_unlock_state.gd`
  - `scripts/tutorial/tutorials/battle_intro_tutorial.gd`

### 更新时间

2026-08-19 20:41 EDT

### 更新内容

- 修复了 Floor 5 中 `OneWay7` 无法向上通过的瓦片冲突问题：
  1. **瓦片冲突排查与消除（`WallLayer` / `GroundLayer`）**：
     - 排查发现 Floor 5 场景中，`OneWay7` 所在坐标 `(1952, 768)`（地砖索引 `[61, 23]`）在 `WallLayer` 上误刷了实体墙体瓦片（`source_id = 0, atlas_coords = (0, 1)`），且 `GroundLayer` 缺失地面瓦片；
     - 导致玩家从下方 `(1952, 800)` 尝试向上进入 `OneWay7` 时，`test_move` 被墙体碰撞体硬拦截；
     - 通过 Godot MCP Pro 对 `scenes/floors/floor_5.tscn` 进行了瓦片修正：清除了 `WallLayer` 在 `[61, 23]` 处的墙体瓦片（`source_id = -1`），并在 `GroundLayer` 补全了可通行的地面瓦片（`source_id = 0, atlas_coords = (0, 0)`）。

### 测试结果

- **通过**：实机物理移动测试验证：玩家在 `(1952, 800)` 向上移动时成功进入 `OneWay7` 并顺势向上滑行至 `(1952, 736)`，单向通道行为完全正常。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260819-204115/reports/`
- 截图证据：`tmp/tests/run-20260819-204115/screenshots/main_scene.png`
- 修改文件：
  - `scenes/floors/floor_5.tscn`

### 更新时间

2026-08-19 20:57 EDT

### 更新内容

- 实现了 `ActionEffectData` 的自由动作（`is_free_action`）通用扩展与战斗结算支持：
  1. **数据模型扩展（`ActionEffectData`）**：
     - 新增 `@export var is_free_action: bool = false`，对标 `is_interrupt` 机制；
     - 在 `get_description()` 中支持自由动作描述生成（`自由动作（不消耗行动机会）`）。
  2. **战斗计算与状态预览（`BattleCalculator`）**：
     - 在 `evaluate_skill_effects()` 与 `evaluate_item()` 中，若检测到 `effect.is_free_action == true`，自动将施法者的 `ActorDelta.is_free_action` 标记为 `true`。
  3. **战斗管理与回合保护（`BattleManager`）**：
     - 在 `_on_item_selected()` 与 `_release_player_skill()` 中，自由动作触发时保持 `_player_atb = ATB_MAX` 并维持 `BattleState.WAITING_FOR_PLAYER`，玩家可立即连续行动；
     - 对自由动作进行了冷却与状态回合保护：仅在非自由动作时推进技能 CD 递减（`_decrement_cooldowns`）与状态回合结算（`on_action_finished`）。
  4. **资源配置更新（`fp_recovery_lv1.tres`）**：
     - 通过 Godot MCP 将 1级专注回复数据的 `ActionEffectData` 子资源配置为 `is_free_action = true`。

### 测试结果

- **通过**：运行时 MCP 断言验证：`BattleCalculator.evaluate_item(fp_item)` 正确计算 `fp_delta = 10.0` 且 `is_free_action = true`；`BattleManager._complete_player_action(true)` 成功保持 `ATB = ATB_MAX` 且状态保持为 `WAITING_FOR_PLAYER`，常规物品依然正常消耗 ATB。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260819-205647/reports/`
- 截图证据：`tmp/tests/run-20260819-205647/screenshots/main_scene.png`
- 修改文件：
  - `scripts/data/action_effect_data.gd`
  - `scripts/battle/battle_calculator.gd`
  - `scripts/battle/battle_manager.gd`
  - `resources/items/fp_recovery_lv1.tres`

### 更新时间

2026-08-19 21:49 EDT

### 更新内容

- 优化了 `ActorStatsPanel` 属性面板中护盾文本格式与进度条宽度布局：
  1. **护盾显示格式简化（`_format_hp_resource`）**：
     - 去除了“盾”/“护盾”字符后缀，将格式统一修改为标准的 `hp (护盾值) / max_hp`（如 `100 (20) / 100`）；
     - 在预览护盾增减时保持紧凑的 `100 (20 (+10)) / 100` 或 `100 (+10) / 100` 格式，文本更加简洁紧凑。
  2. **进度条宽度缩减与排版防溢出（`actor_stats_panel.tscn`）**：
     - 通过 Godot MCP 将 `HpBar`、`MpBar`、`FpBar`、`AtbBar` 的 `custom_minimum_size.x` 从 `250px` 调整为 `180px`；
     - 右侧数值文本栏获得了额外 70px 的横向伸展空间，彻底解决了包含护盾/增减预览时文字过长导致超出属性栏边界的问题。

### 测试结果

- **通过**：运行时 MCP 断言验证：`_format_hp_resource` 输出符合 `100 (35) / 100`，进度条与文本在属性面板内整齐对齐无任何溢出。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260819-214856/reports/`
- 截图证据：`tmp/tests/run-20260819-214856/screenshots/main_scene.png`
- 修改文件：
  - `scripts/ui/components/actor_stats_panel.gd`
  - `scenes/ui/components/actor_stats_panel.tscn`

### 更新时间

2026-08-19 21:53 EDT

### 更新内容

- 修复了通关结算战报生成时 `EquipmentLoadout.get_all_equipped_items()` 不存在的报错问题：
  1. **方法名校正与兼容别名（`EquipmentLoadout` / `SaveManager`）**：
     - 排查发现 `SaveManager.generate_clear_summary_text()` 中调用了不存在的 `_player.equipment.get_all_equipped_items()`，导致 Floor 6 对话结束触发通关结算时报运行时错误并中断了 `GameClearUI` 的弹出；
     - 将 `scripts/save/save_manager.gd` 修正为调用 `get_unique_equipped_items()`；
     - 在 `scripts/inventory/equipment_loadout.gd` 中补充了 `get_all_equipped_items()` 兼容别名方法，彻底消除潜在的方法调用不匹配隐患。

### 测试结果

- **通过**：运行时 MCP 测试验证：触发 `EventBus.game_clear_triggered` 信号后，`GameClearUI` 正常弹出（`visible = true`），成功生成完整通关角色战报摘要文本，上传与复制按钮状态正常。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260819-215314/reports/`
- 截图证据：`tmp/tests/run-20260819-215314/screenshots/main_scene.png`
- 修改文件：
  - `scripts/inventory/equipment_loadout.gd`
  - `scripts/save/save_manager.gd`


### 更新时间

2026-08-20

### 更新内容

- 将通关战报上传从 Google Forms 改为 Discord Incoming Webhook。
- 使用 `multipart/form-data` 上传战报摘要和完整存档 JSON 附件；移除 Google Forms URL 与字段 ID。
- 新增 `config/discord_webhook.cfg.example`，真实 `config/discord_webhook.cfg` 已加入 `.gitignore`；缺少配置时自动复制完整 JSON 到剪贴板。

### 测试结果

失败：脚本通过 Godot headless 编辑器扫描；基础回归 5/8，通过项包括 MCP 连接、项目元数据、主场景启动、MCP Autoload 和正常停止。BR-003 报告编辑器已有 20 个历史错误，BR-005 截图超时，BR-006 运行时场景树无当前场景，均未由本次上传改动直接引入。

### 证据

- 基础回归报告：`tmp/tests/run-20260820-210800/reports/basic_regression.md`
- 配置模板：`config/discord_webhook.cfg.example`
- 代码：`scripts/ui/game_clear_ui.gd`


### 更新时间

2026-08-21 10:48 EDT

### 更新内容

- 完成了玩家 6 槽位携带技能系统、ESC 左右双面板装配界面及战斗无技能自动跳过机制：
  1. **数据驱动互斥组（`SkillData`）**：
     - `SkillData` 增加 `@export var exclusive_group: StringName = &""`；
     - 三个过载技能（`overload_atk.tres`, `overload_def.tres`, `overload_spd.tres`）配置 `exclusive_group = &"overload"`，并在 `get_details()` 增加了专属类别互斥说明。
  2. **角色 6 槽位技能管理与自动装配（`PlayerProgression` / `Player`）**：
     - `PlayerProgression` 维护固定 6 个携带槽位的 `equipped_skills: Array[SkillData]`；
     - 实现了 `can_equip_skill`（互斥拦截与提示）、`equip_skill`（自动移动/互换）、`unequip_skill`、`is_skill_equipped`；
     - 学习新技能时若有空槽且无互斥冲突自动装入槽位，否则仅存入 `learned_skills`；
     - 存档持久化支持 `equipped_skills` 读写，并对旧版本存档提供向后兼容。
  3. **通用 UI 组件化改造（`SkillPanel`）**：
     - 重构为纯 UI 组件，支持普通列表（`display_skills`）与固定 6 槽位列表（`display_slots`）；
     - 支持空槽弱化展示（`[槽位 X] <空槽位>`）、行高亮锁定控制（`set_highlighted_skill` / `set_highlighted_slot` / `clear_highlight`），抛出标准事件信号。
  4. **ESC 技能界面左右双面板交互（`esc_menu.tscn` / `esc_menu.gd`）**：
     - 左侧显示全部已学技能列表（已携带项带 `已携带` 标签），右侧显示 6 个携带槽位；
     - 支持在左侧选定技能锁定高亮后转移到右侧槽位确认装配/替换，支持互斥冲突拦截提示，支持右侧直接按确认键卸下技能，支持全键盘左右无缝导航与取消键退出选定。
  5. **战斗系统联动与无可用技能自动跳过（`battle_ui.gd` / `battle_manager.gd`）**：
     - 战斗面板与被动加载完全基于玩家携带的 6 个槽位技能（`player.get_skills()`）；
     - 轮到玩家行动且检测到携带的主动技能全部不可用（无技能或全在 CD / 资源不足）时，自动触发跳过回合，扣减 CD、结算状态并发送系统提示。
  6. **恢复缺失类**：
     - 恢复了 `TutorialStepData` 脚本类，消除了 Godot 启动时的脚本解析错误。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_system.gd`）：6 槽位限制、自动移动/换位、同组过载技能互斥拦截与提示、卸下技能、新学技能自动填充、ESC 双面板装配交互、战斗技能源与全 CD 自动跳过回合、存档持久化 100% 全部 PASS。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 更新时间

2026-08-21 11:42 EDT

### 更新内容

- 完成了新技能学习提示的教程系统全面整合、专属定制化教学及按键独占模态拦截机制：
  1. **教程 UI 富文本与按键独占拦截（`tutorial_ui.tscn` / `tutorial_ui.gd`）**：
     - 将 `MessageLabel` 从普通 `Label` 升级为支持 BBCode 的 `RichTextLabel`（`bbcode_enabled = true`，`fit_content = true`），支持富文本颜色与高亮标签；
     - 强化独占模态拦截：在教程弹窗等待确认（`_wait_for_confirmation = true`）期间，仅 `ui_accept`（确认键）能关闭弹窗，其他所有按键输入（移动、取消、菜单等）均被 `TutorialUI` 拦截并吞噬，防止误操作。
  2. **通用技能学习教学（`skill_learned_tutorial.gd`）**：
     - 新建 `SkillLearnedTutorial`，监听 `skill_learned` 事件，针对未被专属教程覆盖的新技能弹窗提示：“学会技能【%s】，在菜单界面查看。”；
     - 采用动态 ID `skill_learned_<skill_id>`，确保玩家在各阶段学会的每个新技能均获得一次且仅一次的弹窗教学。
  3. **专属技能教学定制（`interrupt_tutorial.gd` / `fp_skill_tutorial.gd`）**：
     - `kick_off`（打断）：学习时触发专属教程，以富文本红色高亮展示：“学会技能【打断】，[color=#ff4d4f]可以打断敌方的吟唱[/color]，在菜单界面查看。”；
     - `hard_attack`（强力攻击）：学习时展示通用菜单指引，首次进入战斗时触发 `fp_skill_tutorial`，暂停战斗并指引 FP 机制与技能栏 FP 消耗。
  4. **移除冗余飘字通知（`skill_tutor.gd`）**：
     - 移除 `SkillTutor` 学习技能后的重复飘字系统消息，完全由弹出的教程弹窗承接。
  5. **场景与历史清理**：
     - 彻底移除了带有失效资源引用的废弃场景 `scenes/tutorial/tutorial_manager.tscn`。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd`）：TutorialUI RichTextLabel/BBCode 配置、通用技能弹窗文案、独占按键拦截、`kick_off` 红色富文本提示、`hard_attack` 战斗 FP 教学流、已学技能防重复触发 100% 全部 PASS。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-113645/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-113645/screenshots/main_scene.png`
- 修改文件：
  - `scenes/ui/tutorial_ui.tscn`
  - `scripts/ui/tutorial_ui.gd`
  - `scripts/tutorial/tutorials/skill_learned_tutorial.gd` (新建)
  - `scripts/tutorial/tutorials/interrupt_tutorial.gd`
  - `scripts/tutorial/tutorials/fp_skill_tutorial.gd`
  - `scripts/tutorial/tutorial_manager.gd`
  - `scripts/npc/skill_tutor.gd`
  - `tests/test_skill_tutorial.gd` (新建)
  - `scenes/tutorial/tutorial_manager.tscn` (删除废弃文件)

### 更新时间

2026-08-21 11:46 EDT

### 更新内容

- 调整教程弹窗默认位置为屏幕正中央（Center）：
  1. **布局锚点重设（`scenes/ui/tutorial_ui.tscn`）**：
     - 将 `TutorialRoot/CenterContainer` 的 `anchors_preset` 调整为正中居中（`anchor_left = 0.5`, `anchor_top = 0.5`, `anchor_right = 0.5`, `anchor_bottom = 0.5`, `offset_top = -80.0`, `offset_bottom = 80.0`, `grow_vertical = 2`）；
     - 使通用教学弹窗与技能学习弹窗在未指定特定锚定目标控件时，默认在屏幕正中央弹出，显著提升视觉焦点与可读性。

### 测试结果

- **通过**：运行时 MCP 实机渲染与截图验证：教程弹窗在正中心渲染，富文本颜色与自适应高度排版完美，100% PASS。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-114525/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-114525/screenshots/main_scene.png`
- 修改文件：
  - `scenes/ui/tutorial_ui.tscn`

### 更新时间

2026-08-21 11:57 EDT

### 更新内容

- 全面规范化六大基础属性的中英文命名与科幻设定（MP 统一为“灵能 / Mind Power”）：
  - 六大属性标准：
    - `atk` / 攻击
    - `def` / 防御
    - `spd` / 速度
    - `hp` / 生命
    - `mp` / 灵能
    - `fp` / 专注
  1. **UI 场景与面板标签（`actor_stats_panel.tscn` / `entry_info_panel.gd` / `floor_2.tscn`）**：
     - 将 `actor_stats_panel.tscn` 中的 `MpTitle` 文本从“魔力”更新为“灵能”；
     - 物品与装备详情面板（`entry_info_panel.gd`）中的加成与恢复描述统一使用“最大生命 / 最大灵能 / 攻击 / 防御 / 速度 / 专注”；
     - 楼层提示（`floor_2.tscn`）更新为“生命，灵能，专注”。
  2. **数据与效果描述（`action_cost_data.gd` / `action_effect_data.gd` / `status_effect_data.gd`）**：
     - 消耗文案统一生成为 `消耗灵能：%.0f`、`消耗专注：%.0f`、`消耗生命：%.0f`；
     - 效果与状态词条统一格式为 `攻击`、`防御`、`速度`、`最大生命`、`最大灵能`。
  3. **教程系统与提示文案（`mp_skill_tutorial.gd` / `battle_intro_tutorial.gd` / `fp_skill_tutorial.gd` / `fragment_exchanger.gd` / `resources/tutorial/steps/*.tres`）**：
     - 教学文本统一使用“MP（灵能）”、“FP（专注）”、“HP（生命）”、“ATK（攻击）”、“DEF（防御）”、“SPD（速度）”，消除原有散落的“魔法值/法力/魔力/攻击力/防御力/专注值”等不一致。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部通过（PASS），灵能与属性规范文本验证无误。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-123611/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-123611/screenshots/main_scene.png`
- 修改文件：
  - `scenes/ui/components/actor_stats_panel.tscn`
  - `scenes/floors/floor_2.tscn`
  - `scripts/data/action_cost_data.gd`
  - `scripts/data/action_effect_data.gd`
  - `scripts/data/status_effect_data.gd`
  - `scripts/ui/components/entry_info_panel.gd`
  - `scripts/tutorial/tutorials/mp_skill_tutorial.gd`
  - `scripts/tutorial/tutorials/battle_intro_tutorial.gd`
  - `scripts/tutorial/tutorials/fp_skill_tutorial.gd`
  - `scripts/npc/fragment_exchanger.gd`
  - `tests/test_skill_tutorial.gd`
  - `resources/tutorial/steps/show_mp.tres`
  - `resources/tutorial/steps/show_fp.tres`
  - `resources/tutorial/steps/show_hp.tres`
  - `resources/tutorial/steps/show_atk.tres`
  - `resources/tutorial/steps/show_def.tres`
  - `resources/tutorial/steps/show_spd.tres`

### 更新时间

2026-08-21 12:36 EDT

### 更新内容

- 完成技能类别与战斗触发体系的未来科幻重构（`MAGICAL/魔法` → `MIND/灵技`）：
  1. **技能与状态数据枚举重构（`skill_data.gd` / `status_effect_data.gd`）**：
     - `SkillData.SkillType` 中 `MAGICAL` 替换为 `MIND`，`get_type_name()` 返回 `"灵技"`；
     - `StatusEffectData.SkillType` 中 `MAGICAL` 替换为 `MIND`，`get_skill_type_name()` 返回 `"灵技"`；
     - `StatusEffectData.TriggerType` 中 `ON_MAGICAL_ATTACK` / `ON_TAKE_MAGICAL_DAMAGE` 替换为 `ON_MIND_ATTACK` / `ON_TAKE_MIND_DAMAGE`，反伤等动态描述统一生成为 `受到灵技攻击时...`。
  2. **战斗系统与反伤联动（`battle_manager.gd` / `battle_status_controller.gd`）**：
     - 玩家与敌人在释放灵技时触发 `ON_MIND_ATTACK` 状态事件；
     - 受伤反伤判定中基于技能类型派发 `ON_TAKE_PHYSICAL_DAMAGE` 与 `ON_TAKE_MIND_DAMAGE`。
  3. **机关门与通道注释更新（`door.gd` / `portal.gd` / `switch.gd` / `one_way_passage.gd`）**：
     - 将属性机关 `MP` 类型的注释说明统一从 `青色 (魔法)` 更新为 `青色 (灵能)`。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部通过（PASS），灵技技能类型与状态触发逻辑验证无误。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-123611/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-123611/screenshots/main_scene.png`
- 修改文件：
  - `scripts/data/skill_data.gd`
  - `scripts/data/status_effect_data.gd`
  - `scripts/battle/battle_manager.gd`
  - `scripts/battle/battle_status_controller.gd`
  - `scripts/interactables/door.gd`
  - `scripts/interactables/portal.gd`
  - `scripts/interactables/switch.gd`
  - `scripts/interactables/one_way_passage.gd`

### 更新时间

2026-08-21 13:26 EDT

### 更新内容

- 完成技能系统两级分层分类体系重构（`[主动 / 被动] × [物理 / 灵技 / 变化]`）：
  1. **技能数据正交分层（`SkillData`）**：
     - 新增 `ActivationType { ACTIVE, PASSIVE }`（触发类型：主动/被动）；
     - 新增 `SkillDomain { PHYSICAL, MIND, TACTICAL }`（属性领域：物理/灵技/变化）；
     - 增加 `is_passive()`、`get_activation_name()`、`get_domain_name()`；
     - 格式化技能详情中类别显示为两级格式（如：`主动 / 物理`、`主动 / 灵技`、`被动 / 物理`、`被动 / 变化`）。
  2. **状态效果数据重构（`StatusEffectData`）**：
     - 使用 `SkillDomain` 取代原单层 `SkillType`，技能威力增伤/限制判定统一按领域类型匹配。
  3. **战斗系统适配（`battle_ui.gd` / `battle_manager.gd` / `battle_calculator.gd` / `battle_enemy_ai.gd` / `battle_status_controller.gd` / `game_formulas.gd`）**：
     - 战斗面板只载入主动技能（`not skill.is_passive()`）；
     - AI 过滤被动技能（`skill.is_passive()`）；
     - 伤害计算与威力加成按 `skill.domain` 精准关联；
     - 实体初始化被动 Buff 自动挂载由 `skill.is_passive()` 统一管理。
  4. **全量技能资源数据更新（`resources/skills/*.tres`）**：
     - 11 个技能资源全部适配新的 `activation_type` 与 `domain` 字段。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部通过（PASS），技能分层与显示验证无误。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-132619/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-132619/screenshots/main_scene.png`
- 修改文件：
  - `scripts/data/skill_data.gd`
  - `scripts/data/status_effect_data.gd`
  - `scripts/shared/game_formulas.gd`
  - `scripts/battle/battle_ui.gd`
  - `scripts/battle/battle_manager.gd`
  - `scripts/battle/battle_calculator.gd`
  - `scripts/battle/battle_enemy_ai.gd`
  - `scripts/battle/battle_status_controller.gd`
  - `resources/skills/basic_attack.tres`
  - `resources/skills/battle_focus.tres`
  - `resources/skills/data_attack.tres`
  - `resources/skills/hard_attack.tres`
  - `resources/skills/heal.tres`
  - `resources/skills/kick_off.tres`
  - `resources/skills/overflow.tres`
  - `resources/skills/overload_atk.tres`
  - `resources/skills/overload_def.tres`
  - `resources/skills/overload_spd.tres`
  - `resources/skills/thorns.tres`

### 更新时间

2026-08-21 13:49 EDT

### 更新内容

- 完成技能代价（Cooldown）与动作效果体系（ActionEffectData）深度架构重构：
  1. **Cooldown 字段与数据源统一（`SkillData` & `ActionCostData`）**：
     - 从 `SkillData` 中移除冗余的 `@export var cooldown` 字段；
     - 统一由 `costs: Array[ActionCostData]`（`CostType.COOLDOWN`）管理所有冷却；
     - `SkillData.get_cooldown()` 统一查询 `costs` 数组，消灭双重真值来源。
  2. **效果体系三分类重构（`ActionEffectData`）**：
     - 新增 `EffectCategory { RESOURCE, STATUS, SPECIAL }`；
     - 新增 `SpecialType { INTERRUPT, FREE_ACTION }`；
     - 将打断（`INTERRUPT`）与自由动作（`FREE_ACTION`）从数值效果中剥离，独立作为 `SPECIAL` 类型效果；
     - `get_description()` 按类别精确格式化文案，支持多个独立效果自由正交组合。
  3. **战斗计算与物品适配（`battle_calculator.gd` & `fp_recovery_lv1.tres`）**：
     - `evaluate_skill_effects` 与 `evaluate_item` 按 `EffectCategory` 分流处理数值、状态与特殊机制；
     - `fp_recovery_lv1.tres` 拆分为 `RESOURCE (FP 10.0)` 与 `SPECIAL (FREE_ACTION)` 两个独立效果子资源。
  4. **全量技能与物品资源更新**：
     - 更新 `kick_off.tres`（打断与伤害独立）、`battle_focus.tres`、`heal.tres`、`overflow.tres`、`overload_atk.tres`、`overload_def.tres`、`overload_spd.tres`、`thorns.tres`。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部通过（PASS），三分类效果与 Cooldown 数据完全正常。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-134914/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-134914/screenshots/main_scene.png`
- 修改文件：
  - `scripts/data/skill_data.gd`
  - `scripts/data/action_effect_data.gd`
  - `scripts/battle/battle_calculator.gd`
  - `resources/items/fp_recovery_lv1.tres`
  - `resources/skills/battle_focus.tres`
  - `resources/skills/heal.tres`
  - `resources/skills/kick_off.tres`
  - `resources/skills/overflow.tres`
  - `resources/skills/overload_atk.tres`
  - `resources/skills/overload_def.tres`
  - `resources/skills/overload_spd.tres`
  - `resources/skills/thorns.tres`

### 更新时间

2026-08-21 15:05 EDT

### 更新内容

- 彻底重构并优化装备与物品教程交互体系（解决“按确认键无反馈/卡按键”体验问题）：
  1. **首次获得装备引导（`equipment_tutorial.gd`）**：
     - 改为非阻塞分步引导（`wait_for_confirmation = false`，不锁定按键）；
     - 拾取装备 ➔ 提示按 ESC ➔ 打开菜单 ➔ 提示移动焦点到装备 ➔ 移动到装备 ➔ 提示按确认键穿戴 ➔ 穿戴完成自动关闭教程并记录完成。
  2. **后续获得装备通知（`equipment_acquired_tutorial.gd`）**：
     - 首次装备教程完成后，后续每次获得新装备均触发模态通知：`获得新装备【%s】！可在 ESC 菜单中查看并穿戴。`；
     - `wait_for_confirmation = true`，按确认键直接关闭弹窗并恢复行动。
  3. **首次获得 HP 药水引导（`hp_item_tutorial.gd`）**：
     - 非阻塞背包使用分步引导（捡到药水 ➔ 提示按 ESC ➔ 移动焦点到药水 ➔ 提示按确认键在背包使用恢复生命 ➔ 使用或关闭菜单即完成）。
  4. **首次获得 FP 药水引导（`fp_item_tutorial.gd`）**：
     - 非阻塞打开背包 ➔ 模态说明“在战斗中使用 FP 恢复药水是不消耗回合的自由动作（Free Action）” ➔ 按确认关闭。
  5. **后续获得物品零打扰**：
     - 首次引导完成后，后续再次获得 HP / FP 药水或常规道具，不再触发任何多余弹窗。
  6. **TutorialManager 事件与重复性适配**：
     - 挂载 `_esc_menu.closed` ➔ `menu_closed` 事件，防止教程在异常退出菜单时残留；
     - 正确识别 `equipment_acquired_tutorial` 的多次可触发特性。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd` 覆盖 10 项核心教程场景）：全部通过（PASS），非阻塞分步引导与后续模态通知运行完美。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-150442/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-150442/screenshots/main_scene.png`
- 修改文件：
  - `scripts/tutorial/tutorial_manager.gd`
  - `scripts/tutorial/tutorials/equipment_tutorial.gd`
  - `scripts/tutorial/tutorials/equipment_acquired_tutorial.gd`
  - `scripts/tutorial/tutorials/hp_item_tutorial.gd`
  - `scripts/tutorial/tutorials/fp_item_tutorial.gd`
  - `tests/test_skill_tutorial.gd`

### 更新时间

2026-08-21 16:16 EDT

### 更新内容

- 系统消息（System Message）位置重构与轻量化升级：
  1. **位置重定位至 Top Center（屏幕中上方）**：
     - `scenes/ui/game_hud.tscn` 中 `MessageArea` 锚点从 Bottom Center 改为 Top Center（`anchor_left = 0.5, anchor_right = 0.5, offset_top = 28.0, offset_bottom = 76.0`）；
     - 避开玩家角色行进主视线与下方操作区域，贴合现代游戏通知横幅（Toast）交互预期。
  2. **轻量化胶囊样式与尺寸精简**：
     - 尺寸从 `820x96` 厚重终端面板精简为 `560x42` 扁平半透明浮条；
     - 配合 `DarkPanelContainer` / `TransparentPanelContainer` 风格，清爽不遮挡地图与战斗立绘。
  3. **平滑动效（Tween Fade In / Out）**：
     - [`scripts/ui/components/game_message_panel.gd`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/scripts/ui/components/game_message_panel.gd) 接入 Tween 动画；
     - 弹出时 0.15s 平滑渐显（Fade In），展示完毕后 0.25s 渐隐（Fade Out），避免生硬闪烁与突兀消失。

### 测试结果

- **通过**：运行时系统消息触发测试（`EventBus.system_message_requested` 在 Y=28 正常呈现，可见性与文字准确更新）。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-161623/reports/basic_regression.md`
- 截图证据：`tmp/tests/system_message_top.png`
- 修改文件：
  - `scenes/ui/game_hud.tscn`
  - `scenes/ui/components/game_message_panel.tscn`
  - `scripts/ui/components/game_message_panel.gd`

### 更新时间

2026-08-21 16:41 EDT

### 更新内容

- 全局 UI 主题（`game_ui_theme.tres`）与视觉系统深度重构规范化：
  1. **圆角与边框系统统一定义**：
     - 普通面板（`DarkPanelContainer` / `PanelContainer` / `TerminalInsetPanel` 等）：统一圆角 `2px`，边框 `1px`；
     - 大型菜单面板（`TerminalPanel` / `large_menu`）：统一圆角 `3px`，边框 `1px`；
     - 进度条（`ProgressBar` / `HpProgressBar` / `AtkProgressBar` 等）：统一圆角 `1px`，边框 `1px`；
     - 选中 / Focus 交互状态：统一青色 `2px` 边框，圆角 `2px`。
  2. **字体排版与字重体系（FontVariations）**：
     - 普通正文 / 按钮 / 描述：引入 `SourceHanSans-VF.ttf` 字重 `500`（Medium）`FontVariation_regular`，正文字号 `20px`，解决半透明背景上发灰偏细问题；
     - 标题 / 页签 / 栏目：引入字重 `600`（SemiBold）`FontVariation_title`，标题字号 `26px`，栏目字号 `20px`；
     - 辅助文字（`MutedLabel`）：字号 `18px`（不低于 16px），颜色提升至 `#8EA3AA`；
     - 消除大面积依赖描边，保持界面清爽利落。
  3. **交互状态规则与颜色收敛**：
     - 默认：深蓝黑背景 + 低对比灰蓝 1px 边框；
     - Hover：青色 1px 边框 + 微亮青蓝背景；
     - Focus：青色 2px 边框 + 青蓝背景（取消橙色作为所有选中的强调色，避免与 FP 冲突）；
     - Pressed：青色 2px 边框 + 青色高亮背景；
     - Warning（橙色 `#F28A3D`）、Critical（红色 `#F04A5D`）。
  4. **六种核心属性色微调（降低荧光感，更和谐高级）**：
     - `ATK`: `#F04A5D`
     - `DEF`: `#E8BE3A`
     - `SPD`: `#20D9F2`
     - `HP`:  `#45E68A`
     - `MP`:  `#A66BE8`
     - `FP`:  `#F28A3D`
     - 同步更新 `actor_stats_panel.gd`、`level_up_ui.gd`、`npc_interaction_ui.gd`。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部通过（PASS）。
- **通过**：基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全部 PASS，0 失败，Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-164037/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-164037/screenshots/main_scene.png`
- 修改文件：
  - `resources/themes/game_ui_theme.tres`
  - `scripts/ui/components/actor_stats_panel.gd`
  - `scripts/progression/level_up_ui.gd`
  - `scripts/npc/npc_interaction_ui.gd`

### 更新时间

2026-08-21 16:51 EDT

### 更新内容

- UI 色彩常量架构建立与暂停/战斗菜单视觉呼吸感调优：
  1. **建立全局色彩常量库（`scripts/shared/ui_colors.gd`）**：
     - 单一真值来源收录 6 大核心属性色（ATK `#F04A5D`、DEF `#E8BE3A`、SPD `#20D9F2`、HP `#45E68A`、MP `#A66BE8`、FP `#F28A3D`）；
     - 收录文字、背景、边框、状态与数值预览颜色常量，提供 `get_stat_color` 查询工具；
     - 彻底清除 `scripts/ui/components/skill_panel.gd` 中散落的 `Color(1.0, 1.0, 1.0, 0.6)` 等浮点字面量，统一迁移为规范色值；
     - 将技能列表选中与高亮统一采用青色高亮（`UIColors.ACCENT_CYAN`），消除原有的黄色/橙色选中造成的视觉混淆。
  2. **暂停菜单（`scenes/ui/esc_menu.tscn`）空间重构**：
     - 面板尺寸由 `1680×980` 精简调整为 `1600×880`（上下各留 100px，左右各留 160px，解决画面压迫感）；
     - 背景遮罩 Backdrop 透明度从 `0.6` 降低至 `0.45`，大幅增强与游戏地牢世界的通透感；
     - 右侧角色栏由 `480px` 适度收敛为 `440px`，左侧页面列横向扩展至 `980px`，信息密度更饱满舒适。
  3. **战斗 UI（`scenes/battle/battle_ui.tscn`）视觉分层升级**：
     - 战斗主面板由 `1760×900` 调整为 `1640×840`，背景遮罩降低至 `0.48`；
     - 左右两翼玩家/敌人卡片面板宽度收敛至 `440px`，降低边缘视觉抢占；
     - ATB 进度条行动标记采用青色主线（`UIColors.ACCENT_CYAN`），敌方危险吟唱状态专享红色警报（`UIColors.ACCENT_DANGER`）。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部 100% 通过（PASS）。
- **通过**：项目基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全绿通过（PASS），Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-165053/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-165053/screenshots/main_scene.png`
- 修改与新增文件：
  - `scripts/shared/ui_colors.gd` (新建)
  - `scenes/ui/esc_menu.tscn`
  - `scenes/battle/battle_ui.tscn`
  - `scripts/ui/components/skill_panel.gd`
  - `scripts/battle/battle_ui.gd`
  - `scripts/ui/components/actor_stats_panel.gd`
  - `scripts/progression/level_up_ui.gd`
  - `scripts/npc/npc_interaction_ui.gd`

### 更新时间

2026-08-21 18:13 EDT

### 更新内容

- 彻底移除 `TutorialUI` 中独立的 `ContinueLabel` 节点，将确认提示内联至需确认教程的文本末尾：
  1. **UI 场景瘦身与解耦**：
     - [`scenes/ui/tutorial_ui.tscn`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/scenes/ui/tutorial_ui.tscn) 彻底删除静态 `ContinueLabel` 节点；
     - [`scripts/ui/tutorial_ui.gd`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/scripts/ui/tutorial_ui.gd) 移除 `continue_label` 变量引用，保持轻量纯文本驱动；
  2. **教程文本内联确认提示**：
     - 在需要用户按确认键的模态教学文本（`wait_for_confirmation = true`）末尾统一追加 `\n\n[color=#8EA3AA]按确认键继续[/color]`；
     - 涉及教程包括：`battle_intro_tutorial`、`cast_time_tutorial`、`equipment_acquired_tutorial`、`fp_item_tutorial` (阶段2)、`fp_skill_tutorial`、`mp_skill_tutorial`、`interrupt_tutorial`、`skill_learned_tutorial`；
  3. **分步操作指引教程保持无确认提示干预**：
     - `equipment_tutorial`、`hp_item_tutorial`、`fp_item_tutorial` (阶段1) 等分步指引只显示纯粹的操作引导文案。

### 测试结果

- **通过**：专属单元与集成测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部 100% 通过（PASS）。
- **通过**：项目基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全绿通过（PASS），Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-181306/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-181306/screenshots/main_scene.png`
- 修改文件：
  - `scenes/ui/tutorial_ui.tscn`
  - `scripts/ui/tutorial_ui.gd`
  - `scripts/tutorial/tutorials/battle_intro_tutorial.gd`
  - `scripts/tutorial/tutorials/cast_time_tutorial.gd`
  - `scripts/tutorial/tutorials/equipment_acquired_tutorial.gd`
  - `scripts/tutorial/tutorials/fp_item_tutorial.gd`
  - `scripts/tutorial/tutorials/fp_skill_tutorial.gd`
  - `scripts/tutorial/tutorials/mp_skill_tutorial.gd`
  - `scripts/tutorial/tutorials/interrupt_tutorial.gd`
  - `scripts/tutorial/tutorials/skill_learned_tutorial.gd`

### 更新时间

2026-08-21 19:08 EDT

### 更新内容

- 独立战斗消息系统（`CombatMessagePanel`）开发与战斗界面沉浸感升级：
  1. **职责彻底分离（Decoupling）**：
     - 全局 `SystemMessage`（`game_message_panel.gd`）仅用于大地图探索、机关解密、升级开门等探索事件；
     - 移除 `battle_manager.gd` 中残留的 `EventBus.system_message_requested` 战斗相关调用，战斗行为不再污染全局系统消息；
  2. **新建战斗消息浮层组件（`CombatMessagePanel`）**：
     - 新建 [`scripts/ui/components/combat_message_panel.gd`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/scripts/ui/components/combat_message_panel.gd) 与 [`scenes/ui/components/combat_message_panel.tscn`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/scenes/ui/components/combat_message_panel.tscn)；
     - 最大保留 5 条消息（`max_messages = 5`），每条独立驻留 3.8s，淡入 0.15s / 淡出 0.4s，自动平滑向上滚动推移；
     - 溢出挤压机制：瞬间爆发超过 5 条时，最老一条自动加速淡出（0.15s）并释放；
     - 所有容器与标签均设置 `mouse_filter = MOUSE_FILTER_IGNORE`，彻底穿透鼠标与按键，零阻挡战斗操作；
     - 微气泡视觉：半透明深蓝黑背景（`Color(0.04, 0.07, 0.10, 0.72)`）、1px 细边框与 2px 圆角；
  3. **战斗界面（`BattleUI`）空间优化**：
     - [`scenes/battle/battle_ui.tscn`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/scenes/battle/battle_ui.tscn) 移除底部死板的 52px 单行 `MessagePanel`，并将 `CombatMessagePanel` 作为浮层叠加于中央战斗区上方；
     - [`scripts/battle/battle_ui.gd`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/scripts/battle/battle_ui.gd) 对接 `combat_message_panel`；
  4. **全套战斗文案富文本着色化（`UIColors`）**：
     - 技能名称青色、伤害红色、反伤红色、护盾蓝色、打断黄色/橙色、药水绿色、状态提示黄色。

### 测试结果

- **通过**：专属战斗消息测试（[`tests/test_combat_message.gd`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/tests/test_combat_message.gd)）：单条富文本压入、多条 FIFO 容量控制与挤压、全部清空、鼠标穿透全量 PASS。
- **通过**：技能与教程测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部 100% 通过（PASS）。
- **通过**：项目基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全绿通过（PASS），Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-190731/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-190731/screenshots/main_scene.png`
- 新增与修改文件：
  - `scripts/ui/components/combat_message_panel.gd` (新建)
  - `scenes/ui/components/combat_message_panel.tscn` (新建)
  - `tests/test_combat_message.gd` (新建)
  - `scenes/battle/battle_ui.tscn`
  - `scripts/battle/battle_ui.gd`
  - `scripts/battle/battle_manager.gd`
  - `scripts/battle/battle_calculator.gd`
  - `tests/test_skill_tutorial.gd`

### 更新时间

2026-08-21 21:48 EDT

### 更新内容

- 调整 `CombatMessagePanel` 位置至 `bottom center`（底部居中）：
  1. **避免遮挡技能操作区**：
     - [`scenes/battle/battle_ui.tscn`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/scenes/battle/battle_ui.tscn) 将 `CombatMessagePanel` 的锚点由中央（`anchors_preset = 8`）调整为底部居中（`anchors_preset = 7`），水平宽度 680px（对齐 CenterColumn），`offset_top = -220.0`，`offset_bottom = -30.0`，`grow_vertical = 0`；
     - 消息从底部向上堆叠，技能选择列表、敌人卡片与行动条彻底保持完全无遮挡的清爽视野。

### 测试结果

- **通过**：专属战斗消息测试（[`tests/test_combat_message.gd`](file:///Users/yifanli/Github/game/Realmwand_The_First_Tower/tests/test_combat_message.gd)）：全量 PASS。
- **通过**：技能与教程测试（`tests/test_skill_tutorial.gd`、`tests/test_skill_system.gd`）：全部 100% 通过（PASS）。
- **通过**：项目基础回归测试（`python3 tools/run_basic_regression.py`）8/8 项全绿通过（PASS），Godot 编辑器 0 报错。

### 证据

- 基础回归报告：`tmp/tests/run-20260821-214804/reports/basic_regression.md`
- 截图证据：`tmp/tests/run-20260821-214804/screenshots/main_scene.png`
- 修改文件：
  - `scenes/battle/battle_ui.tscn`






